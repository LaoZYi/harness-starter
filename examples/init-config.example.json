{
  "project_name": "Acme API",
  "project_slug": "acme-api",
  "summary": "Handle internal automation requests.",
  "description": "内部自动化请求处理服务，接收来自各业务系统的 webhook，执行预定义的自动化流程，并将结果写回调用方。",
  "features": "接收 webhook 请求并验证签名\n根据 payload 路由到对应处理器\n执行自动化流程并记录执行日志\n将执行结果回调到调用方\n提供执行历史查询 API",
  "constraints": "所有 handler 必须实现幂等性\n不允许在 handler 里直接访问数据库，必须通过 repository 层\n webhook 签名验证不可跳过",
  "project_type": "backend-service",
  "language": "python",
  "package_manager": "uv",
  "run_command": "uv run python -m acme_api",
  "test_command": "uv run pytest",
  "check_command": "uv run ruff check .",
  "ci_command": "make ci",
  "deploy_target": "docker",
  "has_production": true,
  "sensitivity": "internal"
}
