# 知识复利：提取并沉淀经验教训

每一次解决问题都是知识积累的机会。核心原则：**第一次靠研究，记录下来，下一次只需几分钟。**

当前项目：`Agent Harness Framework`（cli-tool / python）
测试命令：`make test`
检查命令：`make check`

## 适用时机

在以下场景完成后运行此命令：
- 修复了一个 Bug
- 解决了一个构建 / 部署问题
- 处理了一个线上事故
- 做出了重要的架构决策
- 发现了一个非显而易见的解决方案

## 第 1 步：上下文分析

回顾刚才完成的工作，明确：

### 1.1 问题描述
- 遇到了什么问题？用一句话概括
- 问题首次出现的时间和场景
- 影响范围：哪些功能 / 用户受到影响？

### 1.2 涉及文件
- 列出修改过的文件路径
- 标记哪些是问题根源，哪些是连带修改
- 记录关键的代码位置（文件:行号）

### 1.3 时间线
- 从发现问题到解决用了多久？
- 中间走过哪些弯路？
- 哪一步花的时间最多？（这往往是最值得记录的部分）

## 第 2 步：解决方案提取

将隐性知识转化为显性记录：

### 2.1 症状
用户或系统看到的表象是什么？包括：
- 错误信息的关键字
- 异常行为的具体表现
- 触发条件

### 2.2 根因
问题的真正原因是什么？
- 不是"改了 X 就好了"，而是"因为 Y 机制导致 Z 条件下触发了问题"
- 区分直接原因和深层原因

### 2.3 解决步骤
按执行顺序列出修复步骤：
1. 第一步做了什么
2. 第二步做了什么
3. ...

**每一步都要具体到可以复制执行的程度。**

### 2.4 预防措施
如何避免同类问题再次发生？
- 需要添加什么测试？
- 需要修改什么配置或流程？
- 需要在代码中加什么防御性检查？

## 第 3 步：查重与合并

在写入之前，**必须**检查已有记录：

```bash
# 搜索已有条目
cat .agent-harness/lessons.md 2>/dev/null | grep -i "<关键词>"
```

### 重叠度判断

| 重叠程度 | 判断标准 | 处理方式 |
|----------|----------|----------|
| 高重叠 | 根因相同，解决方案相似 | **更新已有条目**，补充新发现 |
| 中等重叠 | 相关但根因不同 | **新建条目**，添加交叉引用，标记待合并 |
| 低重叠 | 无明显关联 | **新建条目** |

**铁律：绝不创建重复条目。** 宁可更新旧条目也不要新建相似条目。

## 第 4 步：写入知识库

向 `.agent-harness/lessons.md` 追加结构化条目。如果文件不存在则创建。

### 条目格式

```markdown
## YYYY-MM-DD [分类] 一句话标题

**症状**: 用一到两句话描述可观测到的表象
**根因**: 用一到两句话解释真正的原因
**解决方案**:
1. 步骤一
2. 步骤二
3. ...
**预防措施**: 如何避免再次发生
**相关文件**: `path/to/file1`, `path/to/file2`
```

> **分类前缀必须放在日期后**。memory-index.md 通过扫 `##` 标题提取最近教训，日期在前保证时间排序，`[分类]` 紧随其后使索引中一眼可见归属。

### 可用分类

| 分类 | 适用场景 |
|------|----------|
| 测试 | 测试策略、覆盖率、断言、测试环境、测试数据 |
| 模板 | `templates/` 渲染、占位符、模板变量、dogfood 同步 |
| 流程 | 工作流、任务生命周期、知识库维护、提交/发版约定 |
| 工具脚本 | `scripts/`、`Makefile`、CLI、`check_repo.py`、工具函数重构 |
| 架构设计 | 模块边界、设计权衡、技术选型、接口契约 |
| 集成API | 外部 API（gh / GitLab / 第三方 SDK）、网络依赖、认证 |

> **分类可扩展**：若教训不属于上述 6 类，新增分类并**同步更新本指令表格**和 `lessons.md` 顶部索引。不要把明显不搭的条目塞进已有类。

### 第 4.5 步：更新 `lessons.md` 顶部「按分类索引」

写入条目后，**必须**同步维护 lessons.md 顶部的分类索引块：

```markdown
## 按分类索引

- **测试**: （暂无）
- **模板**: [...], [...]
- **流程**: [新增条目短标题](#YYYY-MM-DD-分类-一句话标题)
- **工具脚本**: ...
- **架构设计**: ...
- **集成API**: ...
```

规则：
1. 新条目的 anchor 根据 GFM 规则生成：heading 小写化 + 空格替换为 `-` + 去方括号。例如 `## 2026-04-12 [测试] 环境变量泄漏` → anchor `#2026-04-12-测试-环境变量泄漏`
2. 将新 anchor 追加到对应分类那一行（用 `, ` 分隔）
3. 若该分类原为「（暂无）」，替换为首个链接
4. 新增分类时同步追加新行

**一致性铁律**：`lessons.md` 条目 + 顶部索引 + `memory-index.md` 三处必须在同一次 commit 中同步，绝不允许其中任何一处滞后。

## 第 5 步：同步更新 memory-index.md

**写完 lessons.md 后，必须同步更新 `.agent-harness/memory-index.md` 的"最近教训"段**，否则分层记忆加载会与实际历史脱节。

### 更新规则

1. 读取 `.agent-harness/memory-index.md`
2. 在 `## 最近教训（保留最多 10 条）` 段顶部插入一行，格式：
   ```
   - YYYY-MM-DD **一句话标题** — 避免什么后果
   ```
   一句话标题即 lessons.md 中的条目标题（去掉分类前缀），"避免什么后果"取自预防措施的精华。
3. 如果该段已满 10 条，**删除最老的那一行**（原 lessons.md 条目保留不动）
4. 如果本次任务同时归档到 task-log（任务真正完成时才会），同步在"最近任务"段顶部插一行，超过 5 条时挤出最老的

### 原子性要求

- lessons.md 新条目 + memory-index.md 索引行必须**在同一次 commit 中完成**，避免历史记录和索引不一致
- 绝不接受"先改 lessons，下次再改索引"——这正是分层记忆的一致性底线

## ADR 联动

如果 `docs/decisions/` 目录下有架构决策记录，检查本次任务是否影响了已有 ADR 的前提条件。如果是，运行 `/adr` 更新相关 ADR 的状态（Proposed → Accepted，或标记为 Deprecated）。

## 关键约束

- **先查重再写入** —— 跳过第 3 步等于制造知识噪音
- **根因必须具体** —— "配置问题"不是根因，"环境变量 X 在 CI 中未设置导致回退到默认值 Y"才是
- **解决步骤必须可执行** —— 读者应该能按步骤直接操作，不需要额外研究
- **每个条目独立可理解** —— 不假设读者了解上下文，必要信息自包含
- **lessons.md 与 memory-index.md 必须同步** —— 任一方缺失或不一致都破坏分层记忆加载的前提
