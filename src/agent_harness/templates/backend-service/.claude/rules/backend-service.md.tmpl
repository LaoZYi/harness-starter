---
description: 后端服务架构和运维约束
paths:
  - "**/routes/**"
  - "**/handlers/**"
  - "**/services/**"
  - "**/middleware/**"
  - "**/controllers/**"
---

# 后端服务规则

本项目是后端服务（{{project_type}}），对外提供接口，对内管理业务逻辑和数据。

## 分层架构

- 入口层（路由/控制器）只做参数校验和响应格式化，禁止包含业务逻辑。因为业务逻辑散落在入口层会导致无法复用和测试。
- 业务层（服务/用例）包含核心逻辑，不直接依赖 HTTP 框架或数据库驱动。因为解耦后可以独立测试，迁移框架时不用重写业务。
- 数据层（仓储/DAO）封装持久化操作，对上层暴露领域接口而非 SQL。

## 健康检查

- 必须提供 `/health` 或等效端点，返回服务是否可用。因为负载均衡和编排系统（k8s、ECS）依赖此端点做流量切换。
- 健康检查应包含关键依赖的连通性验证（数据库、缓存、消息队列），但超时不超过 3 秒。

## 幂等性

- 写操作（POST/PUT/PATCH）应设计为幂等或显式标注非幂等。因为网络重试、消息重放在分布式环境中不可避免。
- 使用幂等键（请求 ID、业务唯一键）防止重复处理。

## 错误处理

- 错误响应使用结构化格式（状态码 + 错误码 + 人类可读消息）。禁止返回裸字符串或框架默认的 HTML 错误页。
- 内部错误（500）禁止暴露堆栈、SQL 语句或内部路径。日志中记录完整信息，响应中只返回错误 ID。

## 日志和可观测性

- 关键操作（用户认��、数据变更、外部调用）必须记录结构化日志，包含请求 ID 和时间戳。
- 禁止在日志中输出密码、token、信用卡号等敏感字段。使用脱敏函数或字段黑名单。

## 限流和降级

- 对外接口必须有限流策略（QPS 上限或令牌桶），超限返回 429 而非让服务被压垮。
- 非核心依赖（推荐、统计、通知）调用失败时走降级逻辑（返回默认值或跳过），不阻塞主流程。
- 外部调用设置超时 + 熔断（连续失败 N 次后短路），防止级联故障。

## 数据库迁移安全

- 迁移脚本必须同时提供 up 和 down。生产环境部署后发现问题需要能回滚。
- 大表加列、改类型、加索引必须评估锁表时间。超过秒级的变更在低峰期执行或使用 online DDL。
- 禁止在迁移中做数据回填（backfill）。数据迁移和 schema 迁移分开执行。

## 检查清单（按需参考）

- 处理认证/授权/输入校验类工作前，查阅 `.agent-harness/references/security-checklist.md`（OWASP Top 10、CORS、Security Headers）
- 排查 N+1、慢查询、接口超时时，查阅 `.agent-harness/references/performance-checklist.md` 的 Backend 段
- 可用 `/recall <关键词> --refs` 定向检索，避免全文读入上下文
