---
description: CLI 工具的用户体验和可靠性约束
paths:
  - "**/cli/**"
  - "**/commands/**"
  - "**/cmd/**"
  - "**/*cli*"
---

# CLI 工具规则

本项目是命令行工具（{{project_type}}），直接在终端与用户交互。

## 退出码

- 成功返回 0，用户级错误返回 1，内部 bug 返回 2。禁止所有错误都返回 1。因为脚本和 CI 依赖退出码判断是否重试。
- 退出码的含义在 `--help` 或文档中说明。

## 输出纪律

- 正常输出写 stdout，错误和诊断信息写 stderr。因为用户可能用管道把 stdout 喂给下一个命令，混入错误信息会破坏管道。
- 可解析输出（JSON/CSV）用 `--format` 或 `--json` 标志控制，默认输出人类可读格式。
- 进度信息、spinner、彩色输出只在 TTY 时启用（检查 `isatty`），管道和文件重定向时自动关闭。

## 参数和帮助

- 每个子命令和选项都必须有帮助文本。`--help` 输出应包含用法示例，而不只是参数列表。
- 必选参数用位置参数，可选配置用 `--flag` 形式。禁止把必选信息藏在环境变量里（可以用环境变量作为默认值，但必须有 flag 覆盖）。
- 参数校验在命令入口处完成，错误信息要说明哪个参数有问题、期望什么值。

## 文件和路径

- 接受用户提供的路径时，始终 resolve 为绝对路径。因为工作目录可能在命令执行中变化。
- 写文件前检查目标路径存在性和权限。覆盖已有文件前必须提示或要求 `--force` 标志。

## 子命令组织

- 子命令按业务领域分组（如 `user list`、`user create`），而不是堆在一级命令下。扁平结构超过 10 个子命令后，`--help` 输出难以扫读。
- 命名一致：统一使用动词-名词或名词-动词顺序，禁止同一工具内混用两种风格。
- 全局选项（`--verbose`、`--config`、`--format`）放在根命令上，子命令只定义自己的专属选项。

## 配置文件处理

- 配置优先级从高到低：命令行标志 → 环境变量 → 配置文件 → 内置默认值。
- 配置文件搜索顺序：当前目录 → 上级目录（向上查找到项目根）→ 用户目录（`~/.config/<tool>/`）→ 系统目录。
- `--config <path>` 标志可显式指定配置文件路径，路径不存在时必须报错，不能静默使用默认值。

## 交互与非交互模式

- 检测 TTY（`isatty(stdin)`）自动选择交互或非交互模式。管道和 CI 环境中必须能无人值守运行。
- 需要用户确认的操作支持 `--yes`（跳过确认）和 `--no-input`（禁止所有交互提示，缺少必要输入时直接报错退出）。
- 交互提示必须写 stderr，不能污染 stdout 的可解析输出。

## Shell 补全

- 为主流 shell（bash、zsh、fish）提供补全脚本或 `completion` 子命令。
- 补全脚本输出到 stdout，用户通过 `eval` 或写入 shell 配置文件启用。

## 向后兼容

- 已发布的子命令和标志名不能在 minor 版本中删除或重命名。废弃时先加 deprecated 警告，下个 major 版本再移除。
- 输出格式的结构性变更（字段名、JSON 结构）视为破坏性变更。
