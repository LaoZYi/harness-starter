---
description: CLI 工具的用户体验和可靠性约束
paths:
  - "**/cli/**"
  - "**/commands/**"
  - "**/cmd/**"
  - "**/*cli*"
---

# CLI 工具规则

本项目是命令行工具（{{project_type}}），直接在终端与用户交互。

## 退出码

- 成功返回 0，用户级错误返回 1，内部 bug 返回 2。禁止所有错误都返回 1。因为脚本和 CI 依赖退出码判断是否重试。
- 退出码的含义在 `--help` 或文档中说明。

## 输出纪律

- 正常输出写 stdout，错误和诊断信息写 stderr。因为用户可能用管道把 stdout 喂给下一个命令，混入错误信息会破坏管道。
- 可解析输出（JSON/CSV）用 `--format` 或 `--json` 标志控制，默认输出人类可读格式。
- 进度信息、spinner、彩色输出只在 TTY 时启用（检查 `isatty`），管道和文件重定向时自动关闭。

## 参数和帮助

- 每个子命令和选项都必须有帮助文本。`--help` 输出应包含用法示例，而不只是参数列表。
- 必选参数用位置参数，可选配置用 `--flag` 形式。禁止把必选信息藏在环境变量里（可以用环境变量作为默认值，但必须有 flag 覆盖）。
- 参数校验在命令入口处完成，错误信息要说明哪个参数有问题、期望什么值。

## 文件和路径

- 接受用户提供的路径时，始终 resolve 为绝对路径。因为工作目录可能在命令执行中变化。
- 写文件前检查目标路径存在性和权限。覆盖已有文件前必须提示或要求 `--force` 标志。

## 向后兼容

- 已发布的子命令和标志名不能在 minor 版本��删除或重命名。废弃时先加 `--deprecated` 警告，下个 major 版本再移除。
- 输出格式的结构性变更（字段名、JSON 结构）视为破坏性变更。
