# 编写实现计划

根据设计文档（或用户描述），生成一份**零上下文可执行**的实现计划。目标：任何人拿到这份计划，不需要额外了解代码库就能按步骤实现。

当前项目：`{{project_name}}`（{{project_type}} / {{language}}）
测试命令：`{{test_command}}`
检查命令：`{{check_command}}`

## 输入来源

按优先级查找计划输入：
1. `docs/superpowers/specs/` 下最新的设计文档
2. `.agent-harness/current-task.md` 中的任务描述
3. 用户在对话中直接描述的需求

如果没有找到任何输入，提示用户先运行 `/brainstorm` 或直接描述需求。

## 计划结构

### 计划头部

```markdown
# 实现计划：<标题>

**目标**：一句话说明要达到的效果
**架构**：涉及的模块和它们的关系
**技术栈**：使用的语言、框架、工具
**设计文档**：<链接到 specs 文件>（如有）
```

### 任务列表

每个任务是一个 2-5 分钟可完成的原子步骤。使用 checkbox 语法。**如果上游有 `/spec` 产出的需求矩阵，每步必须标注实现哪些 R-ID**：

```markdown
## 阶段 1：<阶段描述>

- [ ] 任务 1.1：<动作描述>  ← 实现 **R1, R2**
  - 文件：`<完整文件路径>`
  - 操作：<具体代码变更，给出完整代码块>
  - 测试：新增 `test_xxx`（对应 R1 的测试信号）
  - 验证：`{{test_command}}` 或具体检查命令
  - 预期结果：<明确的通过标准>

- [ ] 任务 1.2：...  ← 实现 **R3**
```

**R-ID 映射规则**：
- 如果上游有 `/spec` 的需求矩阵 → 计划末尾必须有「R-ID 覆盖表」，列出每个 R-ID 被哪个 task 覆盖
- 如果某 R-ID 本次不实施 → 必须在覆盖表中显式标 `out-of-scope`，并说明理由
- 不允许一个 R-ID 无任何 task 映射且不标 out-of-scope（这是 Scope Reduction 反模式）

### 任务粒度要求

遵循 TDD 节奏，每个功能点拆分为：

1. **写失败测试** — 完整的测试代码（不是伪代码）
2. **确认测试失败** — 运行 `{{test_command}}`，确认失败原因正确
3. **实现功能** — 完整的实现代码
4. **确认测试通过** — 运行 `{{test_command}}`，无回归
5. **提交** — 原子 commit，消息说明做了什么

### 代码块要求

**禁止出现以下内容：**
- `// TODO: ...`
- `// 待实现`
- `/* TBD */`
- `...` 省略号代替真实代码
- "参考现有实现" 而不给出具体代码

每个代码块必须是**可直接复制粘贴到文件中的完整代码**。如果代码太长，拆成更小的任务。

### 每个步骤必须包含

1. **精确的文件路径**（从项目根目录开始的相对路径）
2. **完整的代码变更**（新增 / 修改 / 删除）
3. **验证命令**（运行什么命令确认步骤完成）
4. **预期输出**（命令应该输出什么）

## 自审清单

写完计划后，逐条检查：

- [ ] 每个任务都有文件路径
- [ ] 每个任务都有验证命令
- [ ] 代码块没有占位符
- [ ] 任务之间没有隐含依赖（每个任务显式列出前置条件）
- [ ] 计划与设计文档一致（如有设计文档）
- [ ] 总任务量合理（一个功能通常 10-30 个步骤）
- [ ] **R-ID 覆盖表完整**（如上游有需求矩阵）：每个 R-ID 映射到至少一个 task 或标 out-of-scope
- [ ] **文档爆炸门禁**：预计产出文档总数（spec / plan / ADR / review 报告 / current-task 更新等）**统计并展示**

## 文档爆炸门禁（Document Explosion Gate）

> 来源：腾讯 CDN LEGO 团队踩坑——「8 个需求生成 99 个文件，人难以全部确认」。文档过多会**反向降低**审查质量，因为审查者会开始批量跳读。

**硬规则**：

1. 在计划头部显式列出本任务会新增 / 修改的**所有非代码产物**（spec、plan、ADR、报告、lessons 条目、docs 更新等）
2. 总产出 ≥ **10 个非代码文档** 时暂停，展示给用户并询问：
   - 是否能合并同类产物（如 2 个 ADR 合成 1 个）？
   - 是否有 out-of-scope 的产物可以推迟？
   - 接受当前规模继续？
3. 用户确认后才进入执行

**例外**：evolution 模式吸收外部最佳实践、初次搭建知识库等天然需要多文档的场景，用户可显式跳过。

## 输出位置

计划写入以下位置之一：
- `docs/superpowers/specs/YYYY-MM-DD-<topic>-plan.md`
- `.agent-harness/current-task.md`（如果是短期任务）

写入后告知用户计划路径，并提示可以开始按步骤执行。

## 架构决策提醒

如果计划中涉及技术选型或架构模式选择（数据库、框架、API 风格等），在开始执行前先运行 `/adr` 记录决策理由和备选方案。
