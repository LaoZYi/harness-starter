# 代码质量仪表盘

核心原则：**用数字说话，不靠感觉。**

当前项目：`{{project_name}}`（{{project_type}} / {{language}}）
测试命令：`{{test_command}}`
检查命令：`{{check_command}}`

## 六步质量评估

### 第 1 步：探测工具链

根据 `{{language}}` 自动选择检查工具：

| 语言 | 类型检查 | Lint | 测试 | 死代码 |
|------|---------|------|------|--------|
| Python | mypy | ruff / flake8 | pytest | vulture |
| TypeScript/JavaScript | tsc | eslint | jest / vitest | ts-prune |
| Go | go vet | golangci-lint | go test | deadcode |
| Rust | cargo check | clippy | cargo test | — |
| Java | javac | checkstyle | mvn test | — |

如果项目有自定义配置（pyproject.toml、.eslintrc、tsconfig.json 等），优先使用项目配置。

### 第 2 步：逐项运行

按顺序运行以下工具，捕获退出码和输出：

1. **类型检查**：运行语言对应的类型检查器
2. **Lint**：运行 `{{check_command}}`，如果项目另有 lint 工具也运行
3. **测试**：运行 `{{test_command}}`，记录通过/失败/跳过的计数
4. **死代码检测**：运行对应工具扫描未使用的导出和函数
5. **Shell 脚本检查**：如果项目有 `.sh` 文件，运行 `shellcheck`

每项运行后记录：
- 退出码（0 = 成功）
- 错误/警告数量
- 关键输出摘要

### 第 3 步：分项评分（0-10）

| 类别 | 权重 | 10 分 | 7 分 | 4 分 | 0 分 |
|------|------|-------|------|------|------|
| 类型检查 | 25% | 零错误 | <10 错误 | <50 错误 | ≥50 错误 |
| Lint | 20% | 零警告 | <5 警告 | <20 警告 | ≥20 警告 |
| 测试 | 30% | 全部通过 | >95% 通过 | >80% 通过 | ≤80% 通过 |
| 死代码 | 15% | 零未使用 | <5 未使用 | <20 未使用 | ≥20 未使用 |
| Shell | 10% | 零问题 | <5 问题 | — | ≥5 问题 |

注意事项：
- 如果某项工具不适用（比如没有 Shell 脚本），将其权重重新分配到其他项
- 测试权重最高，因为它直接反映行为正确性

### 第 4 步：计算综合得分

```
综合得分 = Σ (分项得分 × 权重)
```

### 第 5 步：输出仪表盘

使用以下格式输出结果：

```markdown
# 代码质量报告 — {{project_name}}

| 类别 | 得分 | 状态 | 详情 |
|------|------|------|------|
| 类型检查 | X/10 | STATUS | N 个错误 |
| Lint | X/10 | STATUS | N 个警告 |
| 测试 | X/10 | STATUS | X/Y 通过 |
| 死代码 | X/10 | STATUS | N 个未使用 |
| Shell | X/10 | STATUS | N 个问题 |
| **综合** | **X.X/10** | **STATUS** | — |
```

状态标签：
- 10：`CLEAN`
- 7-9：`WARNING`
- 4-6：`NEEDS WORK`
- 0-3：`CRITICAL`

### 第 6 步：改进建议

按影响排序给出建议（优先修复得分最低的类别）：

```markdown
## 改进建议（按优先级排列）

1. **[类别名] (当前 X/10 → 目标 Y/10)**
   - 具体问题：...
   - 修复方法：...
   - 预估工作量：...

2. ...
```

## 可选：记录到项目

如果项目存在 `.agent-harness/` 目录，将本次评分追加到 `.agent-harness/lessons.md`：

```markdown
## 质量快照 — YYYY-MM-DD
- 综合得分：X.X/10
- 最弱项：[类别] (X/10)
- 趋势：↑/↓/→（与上次对比）
```

## 一句话总结

**运行工具 → 统计数字 → 算出得分 → 找出最弱项 → 给出修复方案。**
