# 完成开发分支

你的任务是安全地完成当前开发分支的工作，提供结构化的收尾选项。

项目：`{{project_name}}`（{{project_type}}，{{language}}）

## 铁律

- **测试未通过时绝不合并**
- **丢弃操作需要用户输入 "discard" 确认**
- **合并前必须再次验证测试**

## 第 1 步：验证测试状态

运行完整测试套件：

```bash
{{test_command}}
```

- **全部通过**：继续下一步
- **有失败**：列出失败测试，**停止流程**。告知用户必须先修复测试才能继续完成分支

## 第 2 步：检测基础分支

自动检测当前分支的起始点：

1. 检查常见主分支名：`main`、`master`、`develop`
2. 使用 `git log --oneline --graph` 查看分支历史
3. 使用 `git merge-base` 确认分叉点
4. 如果无法确定，询问用户

记录：当前分支名、基础分支名、提交数量、变更文件数。

## 第 3 步：展示选项

向用户展示 4 个选项：

```
## 分支完成选项

当前分支：<分支名>（基于 <基础分支>，共 N 个提交）
变更文件：M 个

请选择操作：

1. 本地合并 —— 将分支合并回 <基础分支>，删除分支和 worktree
2. 推送并创建 PR —— 推送到远程，创建 Pull Request
3. 保留分支 —— 不做任何操作，保留分支和 worktree 供后续使用
4. 丢弃分支 —— 删除所有变更（需要输入 "discard" 确认）
```

等待用户选择后执行。

## 选项 1：本地合并

1. 再次运行 `{{test_command}}` 验证测试
2. 切换到基础分支：`git checkout <基础分支>`
3. 合并：`git merge --no-ff <分支名>`
4. 删除分支：`git branch -d <分支名>`
5. 清理 worktree（如果在 worktree 中工作）
6. 运行 `{{check_command}}` 最终验证

## 选项 2：推送并创建 PR

1. 再次运行 `{{test_command}}` 验证测试
2. 推送分支：`git push -u origin <分支名>`
3. 使用 `gh pr create` 创建 PR，包含：
   - 自动生成的标题（从分支名和提交信息提炼）
   - 变更摘要（列出主要修改）
   - 测试状态说明
4. 清理 worktree（如果在 worktree 中工作）
5. 向用户展示 PR 链接

## 选项 3：保留分支

- 不执行任何操作
- **不清理 worktree**——用户可能还需要继续工作
- 告知用户分支和 worktree 的位置

## 选项 4：丢弃分支

1. **要求用户输入 "discard" 确认**——不接受 y/yes 等简写
2. 确认后：切换到基础分支
3. 删除分支：`git branch -D <分支名>`
4. 清理 worktree（如果在 worktree 中工作）
5. 报告已清理完毕

## Worktree 清理

当需要清理 worktree 时（选项 1、2、4）：

```bash
git worktree remove <worktree路径>
```

如果 worktree 有未提交的变更，**先警告用户**再决定是否强制清理。

## 完成报告

```
## 分支完成报告

- 执行操作：<选项描述>
- 分支状态：已合并 / 已推送 / 已保留 / 已删除
- Worktree：已清理 / 已保留
- 最终验证：通过 / 不适用
```
