# LFG — 从计划到交付的全自动流水线

铁律：**先有计划，再动手。没有例外。**

当前项目：`{{project_name}}`（{{project_type}} / {{language}}）
测试命令：`{{test_command}}`
检查命令：`{{check_command}}`
启动命令：`{{run_command}}`

## 流水线总览

```
计划 → 实施 → 评审 → 修复 → 验证 → 沉淀 → 收尾
```

每个阶段必须顺序执行，不得跳过。每个阶段设有通过门，未通过则不能进入下一阶段。

---

## 阶段 1：计划

**通过门：`docs/superpowers/specs/` 下存在对应的计划文件**

1. 检查 `docs/superpowers/specs/` 是否已有与当前任务相关的计划文件
2. **如果没有计划**：运行 `/write-plan` 生成计划，等待计划写入完成
3. **如果已有计划**：阅读并确认计划内容是否仍然适用
4. 确认计划文件路径，记录到 `.agent-harness/current-task.md`

**绝对禁止：**
- 没有计划就开始写代码
- 口头描述代替书面计划
- 跳过计划直接进入实施

---

## 阶段 2：实施

**通过门：所有计划步骤标记为完成，测试通过**

1. 根据计划复杂度选择执行方式：
   - TDD 适用的功能：运行 `/tdd`
   - 非 TDD 任务（配置、迁移等）：运行 `/execute-plan`
2. 每完成一个原子步骤，创建一次 commit
3. 在 `.agent-harness/current-task.md` 中更新进度
4. 每步完成后运行 `{{test_command}}` 确保无回归

遇到阻塞时：记录阻塞原因，停止实施，向用户报告。

---

## 阶段 3：评审

**通过门：评审结论为 PASS 或 PASS WITH CONDITIONS**

1. 运行 `/multi-review` 对当前分支的所有变更进行评审
2. 收集评审报告，记录所有 P0 和 P1 发现
3. 如果评审结论为 PASS 且无 P0/P1 → 直接进入阶段 5（验证）
4. 如果存在 P0 或 P1 → 进入阶段 4（修复）

---

## 阶段 4：修复

**通过门：重新评审结论为 PASS，或达到最大修复轮次**

修复循环（最多 3 轮）：

```
发现问题 → 修复 → 重新评审 → 检查是否通过
```

每一轮：
1. 按 P0 → P1 的优先级修复发现的问题
2. 每个修复创建独立的 commit，消息说明修复了什么
3. 运行 `{{test_command}}` 确认修复没有引入新问题
4. 重新运行 `/multi-review` 评审

**3 轮后仍有 P0/P1 → 立即停止，向用户报告：**

```
## 修复循环未收敛

已执行 3 轮修复，仍存在以下问题：
- <问题列表>

建议操作：
1. 人工审查上述问题
2. 可能需要调整实现方案
3. 考虑回退到计划阶段重新设计
```

---

## 阶段 5：验证

**通过门：所有验证命令通过**

运行 `/verify` 执行完整验证：

1. `{{test_command}}` — 全部测试通过
2. `{{check_command}}` — 代码质量检查通过
3. `{{run_command}}` — 构建/启动验证（如适用）

任何验证失败 → 回到阶段 4 修复，计入修复轮次。

---

## 阶段 6：沉淀

**通过门：经验教训已记录**

运行 `/compound`（如可用）或手动执行：

1. 回顾本次任务的关键决策
2. 记录遇到的问题和解决方案
3. 提取可复用的模式或知识
4. 更新项目文档（如有需要）

---

## 阶段 7：收尾

根据工作方式选择收尾操作：

- **在 worktree 中工作**：运行 `/finish-branch`，选择合并或推送 PR
- **在主分支工作**：运行 `/git-commit` 创建最终 commit
- 确保所有变更已提交，工作区干净

---

## 全局约束

1. **计划优先**：任何情况下都不能跳过计划阶段
2. **修复上限**：评审-修复循环最多 3 轮，超过则停止并上报
3. **不猜测**：遇到不确定的问题，停下来问用户
4. **原子提交**：每个有意义的变更都创建独立 commit
5. **证据驱动**：所有"通过"结论必须有命令输出作为证据
6. **沉淀闭环**：每次任务结束都要提取经验教训

## 进度追踪

在 `.agent-harness/current-task.md` 中记录当前阶段：

```markdown
## LFG 进度

- [x] 计划 — docs/superpowers/specs/xxx-plan.md
- [x] 实施 — 12 个步骤，12 个 commit
- [ ] 评审 — 进行中...
- [ ] 修复
- [ ] 验证
- [ ] 沉淀
- [ ] 收尾
```
