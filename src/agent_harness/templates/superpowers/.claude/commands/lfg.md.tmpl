# LFG — 从需求到交付的全自动流水线

铁律：**先有计划，再动手。没有例外。**

当前项目：`{{project_name}}`（{{project_type}} / {{language}}）
测试命令：`{{test_command}}`
检查命令：`{{check_command}}`
启动命令：`{{run_command}}`

---

## 阶段 0：复杂度评估（自动）

收到任务后，先判断复杂度，决定走哪条流水线：

| 级别 | 判断标准 | 走哪条线 |
|------|---------|---------|
| **微小** | 改 1-2 个文件，无行为变化（typo、配置、格式） | 快速通道：直接改 → 验证 → 提交 |
| **小** | 改 1-3 个文件，行为变化明确（bug fix、加字段） | 轻量通道：跳过构思，计划→实施→验证→提交 |
| **中** | 改 3+ 个文件，需要设计（新功能、新接口） | 标准通道：完整 7 阶段 |
| **大** | 跨模块、需要架构决策、影响面广 | 完整通道：加构思 + worktree 隔离 |

**判断方法**：读任务描述 + 扫描涉及的代码范围，然后告诉用户判断结果和走哪条线。用户可以覆盖（比如"这个虽然只改一个文件但很关键，走标准通道"）。

---

## 快速通道（微小任务）

```
直接修改 → {{test_command}} → {{check_command}} → /git-commit
```

不写计划、不评审。完成后如有值得记录的发现，运行 `/compound`。

---

## 轻量通道（小任务）

```
计划 → 实施 → 验证 → 提交
```

- 计划：在 `.agent-harness/current-task.md` 中写 3-5 行计划（不生成独立文件）
- 实施：`/tdd`
- 验证：`{{test_command}}` + `{{check_command}}`
- 提交：`/git-commit`
- 沉淀：运行 `/compound`（如果解决了值得记录的问题）

---

## 标准/完整通道

### 阶段 1：环境准备（自动 | 完整通道专属）

1. 记录当前分支和 HEAD commit（用于回滚）
2. **创建 worktree 隔离环境**：
   - 运行 `/use-worktrees` 创建新分支和工作目录
   - 在 worktree 中运行 `{{test_command}}` 确认基线测试通过
   - 如果基线测试失败 → **停下来告诉用户**，不在一个已经有问题的基础上工作
3. 记录质量基线：运行 `{{test_command}}` 和 `{{check_command}}`，记录结果（用于阶段 7 对比）

**⚙️ 自动执行，不需要用户确认。**

> 如果用户明确说"不用 worktree"或任务是中等复杂度，可以跳过 worktree，直接在当前分支工作。

---

### 阶段 2：构思（需确认 | 完整通道专属）

仅在**复杂度为"大"**时执行：

1. 运行 `/ideate` 多角度探索方案
2. 展示候选方案给用户
3. **🔴 等待用户选择方向**
4. 运行 `/brainstorm` 深入设计选定方案
5. 设计文档写入 `docs/superpowers/specs/`

**标准通道跳过此阶段。**

---

### 阶段 3：计划（需确认）

**通过门：计划文件存在且通过审查**

1. 检查 `docs/superpowers/specs/` 是否已有适用的计划
2. **如果没有**：运行 `/write-plan` 生成计划
3. **计划审查**：对计划做快速检查——
   - 是否覆盖了需求的所有方面？
   - 步骤是否足够具体（有文件路径、有验证命令）？
   - 是否有遗漏的边界情况？
   - 如果发现问题，修正计划
4. **🔴 将计划摘要展示给用户确认**（显示步骤数、涉及文件、预计影响范围）
5. 用户确认后记录计划路径到 `.agent-harness/current-task.md`

**绝对禁止：**
- 没有计划就开始写代码
- 口头描述代替书面计划
- 不审查就直接执行计划

---

### 阶段 4：实施（自动）

**通过门：所有计划步骤完成，测试通过**

1. 根据每个步骤的性质选择执行方式：
   - 写功能代码 → `/tdd`（先写测试，再写实现）
   - 配置/迁移/脚本 → `/execute-plan`（按步骤执行）
2. **每完成一个原子步骤**：
   - 运行 `{{test_command}}` 确认无回归
   - 创建一次 commit（消息说明做了什么）
   - 在 `.agent-harness/current-task.md` 更新进度复选框
3. **遇到阻塞时**：记录原因，**🔴 停下来告诉用户**，不要猜测或绕过

**⚙️ 正常情况自动执行。遇阻或不确定时停下。**

---

### 阶段 5：评审（自动 → 可能需确认）

**通过门：评审结论为 PASS**

1. 运行 `/multi-review` 对所有变更做 6 角色并行评审
2. 收集评审报告

根据结果分流：

| 结论 | 下一步 |
|------|--------|
| **PASS**（无 P0/P1） | → 跳到阶段 7（验证） |
| **PASS WITH CONDITIONS**（有 P1 但可修） | → 进入阶段 6（修复） |
| **FAIL**（有 P0） | → 进入阶段 6（修复） |

**⚙️ 自动执行，不需要确认。**

---

### 阶段 6：修复循环（自动 → 可能需确认）

**通过门：重新评审 PASS，或达到 3 轮上限**

每一轮：
1. 按 P0 → P1 优先级修复
2. 每个修复创建独立 commit
3. 运行 `{{test_command}}` 确认修复没有引入新问题
4. 重新运行 `/multi-review`
5. 如果 PASS → 结束循环

**3 轮后仍有 P0/P1 → 🔴 停下来，向用户报告：**

```
⚠️ 修复循环未收敛（已执行 3 轮）

仍存在的问题：
- [P0] file.py:42 — 空指针未处理
- [P1] api.py:88 — 缺少输入验证

建议：
1. 人工审查上述问题
2. 可能需要调整实现方案
3. 输入 "继续" 再试 3 轮，或 "回退" 撤销到阶段 1 的基线
```

---

### 阶段 7：验证（自动）

**通过门：所有验证通过**

1. `{{test_command}}` — 全部测试通过
2. `{{check_command}}` — 代码质量检查通过
3. 如果项目 `has_production` 为 true → 运行 `/cso` 做安全审计
   - 安全审计发现 Critical/High → **🔴 停下来告诉用户**
   - 安全审计只有 Medium/Low → 记录到报告，继续

**验证失败 → 回到阶段 6 修复（计入修复轮次）。**

---

### 阶段 8：质量对比（自动）

将当前结果与阶段 1 记录的质量基线对比：

```
## 质量变化

| 指标 | 之前 | 之后 | 变化 |
|------|------|------|------|
| 测试数 | 42 | 48 | +6 ✅ |
| 测试通过率 | 100% | 100% | 不变 |
| Lint 警告 | 3 | 2 | -1 ✅ |
| 类型错误 | 0 | 0 | 不变 |
```

如果质量变差（测试减少、警告增多）→ 在报告中标记，但不阻塞。

---

### 阶段 9：沉淀（自动）

1. 运行 `/compound` 提炼本次任务的经验教训
2. 回顾：遇到了什么问题？怎么解决的？下次怎么避免？
3. 写入 `.agent-harness/lessons.md`

**⚙️ 自动执行。**

---

### 阶段 10：收尾与文档（自动 → 需确认）

1. **收尾**：
   - 在 worktree 中 → 运行 `/finish-branch`，**🔴 让用户选择**：合并 / 创建 PR / 保留 / 丢弃
   - 在主分支 → 运行 `/git-commit` 创建最终 commit
2. **文档同步**：运行 `/doc-release` 检查并更新项目文档
3. **完成报告**：

```
## ✅ LFG 完成报告

任务：<任务描述>
复杂度：<中/大>
通道：<标准/完整>

计划：docs/superpowers/specs/xxx-plan.md
提交：<commit 数> 个 commit（<SHA 范围>）
评审：PASS（第 <N> 轮）
安全：<通过 / 未执行>
质量变化：测试 +6，Lint 警告 -1

沉淀：1 条经验写入 lessons.md
文档：README.md, docs/product.md 已同步
```

---

## 进度追踪与中断恢复

在 `.agent-harness/current-task.md` 中实时记录：

```markdown
## LFG 进度

任务：给用户列表页增加搜索功能
复杂度：中
通道：标准
基线 commit：abc1234
Worktree：.worktrees/feat-user-search（如果使用）

- [x] 环境准备 — 基线测试通过，质量快照已记录
- [x] 计划 — docs/superpowers/specs/2026-04-07-user-search-plan.md
- [x] 实施 — 8/8 步骤完成，8 个 commit
- [x] 评审 — 第 1 轮 PASS WITH CONDITIONS → 修复 1 轮 → 第 2 轮 PASS
- [x] 验证 — pytest ✓ (48/48), ruff ✓
- [x] 质量对比 — 测试 +6, Lint -1
- [x] 沉淀 — SQL 注入防护经验写入 lessons.md
- [ ] 收尾 — 待用户选择合并方式
```

**中断恢复**：如果会话中断，下次打开 Claude Code 时 AI 会读取 `current-task.md`，看到 LFG 进度，从最后一个未完成的阶段继续。

---

## 用户确认点汇总

| 阶段 | 是否需要确认 | 什么时候停下 |
|------|-------------|-------------|
| 复杂度评估 | ⚙️ 自动（用户可覆盖） | 展示判断结果，用户可说"走完整通道" |
| 环境准备 | ⚙️ 自动 | 基线测试失败时 |
| 构思 | 🔴 需确认 | 用户选择方向 |
| 计划 | 🔴 需确认 | 展示计划摘要等用户确认 |
| 实施 | ⚙️ 自动 | 遇阻或不确定时 |
| 评审 | ⚙️ 自动 | 不停 |
| 修复 | ⚙️ 自动 | 3 轮未收敛时 |
| 验证 | ⚙️ 自动 | 安全审计发现 Critical/High 时 |
| 质量对比 | ⚙️ 自动 | 不停 |
| 沉淀 | ⚙️ 自动 | 不停 |
| 收尾 | 🔴 需确认 | 选择合并方式 |
