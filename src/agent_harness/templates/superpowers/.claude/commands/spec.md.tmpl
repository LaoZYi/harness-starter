# 规格驱动开发（Spec-Driven Development）

铁律：**先有规格，再动手。需求不清就写代码，等于蒙眼开车。**

当前项目：`{{project_name}}`（{{project_type}} / {{language}}）
测试命令：`{{test_command}}`

## 适用场景

以下情况**必须**先写规格再动手：

- 新功能开发（需求超过一句话能描述的）
- 跨模块改动（涉及 3+ 个文件）
- 需求模糊或来自非技术人员的描述
- 预计开发时间超过 30 分钟

例外情况（可跳过规格）：
- 单行修复（typo、配置值）
- 已有明确验收标准的 Bug 修复
- 纯重构（行为不变）

## 四阶段门控流程

```
规格定义 → 用户确认 → 技术计划 → 任务拆解 → 实施
```

每个阶段必须通过用户确认才能进入下一阶段。

---

### 阶段 1：规格定义（Specify）

#### 1.1 先列假设

在写任何规格内容之前，先显式列出所有假设：

```
## 假设清单
- 这是一个 Web 应用（不是原生移动端）
- 认证使用 session-based cookies（不是 JWT）
- 数据库使用 PostgreSQL
- 目标用户是内部员工
```

> 规格的全部目的是在代码写出来之前暴露误解。而假设是最危险的误解形式。

#### 1.2 六个核心区域

逐一填写以下六个区域，每个都不能留空：

**1. 目标（Objective）**
- 要构建什么？
- 为什么要构建？（业务动机）
- 目标用户是谁？
- 怎么定义成功？（可量化的标准）

**2. 命令（Commands）**
- 构建命令：`...`
- 测试命令：`{{test_command}}`
- 代码检查命令：`{{check_command}}`
- 开发启动命令：`...`

**3. 项目结构（Project Structure）**
- 新增或修改的目录和文件
- 每个文件的职责（一句话）

**4. 代码风格（Code Style）**
- 用真实代码片段展示约定（不要抽象描述）
- 命名规范、导入顺序、错误处理模式

**5. 测试策略（Testing Strategy）**
- 使用的测试框架
- 测试文件放在哪里
- 必须覆盖的场景：正常路径、边界情况、错误路径

**6. 边界（Boundaries）**

| 类别 | 内容 |
|------|------|
| **始终做** | 写测试、遵循代码规范、验证输入 |
| **先问再做** | 修改数据库 schema、引入新依赖、改 CI 配置 |
| **绝不做** | 提交密钥、修改 vendor 代码、删除失败的测试 |

#### 1.3 把模糊需求转为可测标准

遇到模糊需求时，必须转化为可测试的验收标准：

| 模糊需求 | 转化后的标准 |
|---------|------------|
| "让页面加载更快" | LCP < 2.5s，首屏加载 < 500ms |
| "支持搜索" | 按姓名模糊搜索，结果在 200ms 内返回，空结果有提示 |
| "提升安全性" | 所有输入做转义，API 端点加认证，密码 bcrypt 存储 |

**🔴 完成后展示规格，等待用户确认。**

---

### 阶段 2：技术计划（Plan）

在规格确认后，生成技术实施路线图：

1. 识别主要组件和依赖关系
2. 确定实施顺序（哪些可以并行，哪些必须串行）
3. 列出风险和缓解措施
4. 标注验证检查点

**🔴 展示计划，等待用户确认。**

---

### 阶段 3：任务拆解（Tasks）

将计划拆分为离散的、可独立完成的任务：

- 每个任务有明确的验收标准
- 每个任务有验证步骤（测试、构建、检查）
- 任务之间的依赖顺序清楚
- 每个任务影响的文件不超过 5 个

---

### 阶段 4：实施（Implement）

按任务顺序逐个实施：

1. 使用 `/tdd` 进行测试驱动开发
2. 每完成一个任务运行 `{{test_command}}` 确认无回归
3. 每个任务创建一个原子 commit
4. 实施过程中发现规格需要调整 → 更新规格文档，注明变更原因

## 规格是活文档

规格不是写完就扔的：

- 技术决策变更时 → 更新规格
- 范围扩大或缩小时 → 更新规格
- 实施中发现遗漏 → 更新规格
- 代码提交时，规格也要同步提交

## 规格输出位置

规格文档写入 `docs/superpowers/specs/YYYY-MM-DD-<topic>-spec.md`。

## 反合理化

| 你想说的 | 为什么不行 |
|---------|-----------|
| "太简单了，不需要规格" | 简单的任务也需要验收标准。没有验收标准就无法判断"完成" |
| "我先写代码再补规格" | 那是文档，不是规格。规格的价值在于写代码之前理清思路 |
| "写规格太慢了" | 15 分钟的规格能省掉几小时的返工。方向错了跑得再快也是浪费 |
| "需求会变的，写了也白写" | 规格是活文档，随需求更新。过时的规格仍然比没有规格好 |
| "需求很明确，没什么好写的" | 隐含假设无处不在。你觉得明确的地方，恰恰可能藏着误解 |

## 一句话总结

**先明确"做什么"和"做到什么程度"，再考虑"怎么做"。**
