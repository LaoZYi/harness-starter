# 头脑风暴：结构化设计对话

在编写任何代码之前，通过结构化对话完成方案设计。铁律：**设计未获批准前，禁止实现。**

当前项目：`{{project_name}}`（{{project_type}} / {{language}}）

## 执行流程（9 步）

### 第 1 步：探索项目上下文

在讨论任何方案之前，先了解现状：
- 读取项目目录结构和关键文件
- 查看最近 10 条 commit，理解项目演进方向
- 识别现有架构模式、约定和技术栈
- 找到与本次设计相关的已有代码

输出一段简要的上下文总结，确认你理解了项目现状。

### 第 2 步：澄清问题

**每次只问一个问题。** 不要一次抛出问题列表。

需要澄清的方向：
- 用户真正想解决的问题是什么（而非他们提出的方案）
- 使用场景和边界条件
- 性能、安全、兼容性方面的约束
- 与现有系统的交互方式

持续追问直到你能清晰地复述问题。

### 第 3 步：复述问题

用一段话总结你理解的问题。等待用户确认或纠正。

### 第 4 步：提出方案

提出 2-3 个可行方案，每个方案包括：
- **方案名称**：一句话描述核心思路
- **实现概要**：关键组件和数据流
- **优势**：这个方案好在哪里
- **劣势和风险**：可能的坑
- **工作量估算**：大致规模（小 / 中 / 大）

### 第 5 步：对比讨论

用表格对比方案的关键维度：复杂度、可维护性、扩展性、对现有代码的侵入性。

推荐一个方案并说明理由。等待用户选择或提出修改意见。

### 第 6 步：细化设计

用户选定方案后，分模块展开设计细节：
- 数据模型 / 接口定义
- 核心流程和状态变迁
- 错误处理策略
- 边界情况的处理方式

**分段展示，每段等待确认后再继续。** 不要一次输出完整设计。

### 第 7 步：自审

在定稿前检查设计文档：
- [ ] 没有占位符（"待定"、"TBD"、"后续补充"）
- [ ] 没有内部矛盾
- [ ] 每个模块的职责清晰
- [ ] 错误路径有明确处理方式
- [ ] 与项目现有模式一致

### 第 8 步：写入设计文档

将批准的设计写入：

```
docs/superpowers/specs/YYYY-MM-DD-<topic>-design.md
```

文档结构：
```markdown
# <主题> 设计方案

## 背景与问题
## 方案概述
## 详细设计
### 数据模型
### 核心流程
### 错误处理
## 被否决的方案（及原因）
## 开放问题
```

### 第 9 步：过渡到实现

设计文档写入后，提示用户：

> 设计已保存。如果本次讨论产生了架构级决策（技术选型、模式选择），先运行 `/adr` 记录决策，再运行 `/write-plan` 生成实现计划。

## 关键约束

- **禁止在设计阶段写任何实现代码**
- 每个环节都要等待用户确认
- 如果用户直接要求写代码，提醒他们先完成设计
- 所有设计决策必须有理由，不接受"通常这样做"作为理由
