# 使用 Superpowers 技能系统

你的任务是根据当前工作内容，识别并调用最合适的技能（skill）。

项目：`{{project_name}}`（{{project_type}}，{{language}}）

## 核心规则

**如果某个技能适用于你的任务，你必须使用它。**

这不是建议，是强制要求。

## 指令优先级

从高到低：

1. **用户直接指令** —— 用户说的话永远最优先
2. **Superpowers 技能规则** —— 技能中定义的流程和约束
3. **系统提示** —— 默认行为和通用指令

当层级间出现冲突时，遵循高优先级的指令。

## 1% 法则

> 只要有 1% 的可能性某个技能与当前任务相关，就必须调用它。

不要自己判断"这次不需要"。宁可多调用一个不需要的技能，也不要漏掉一个需要的。

## 技能调用顺序

### 先调用流程类技能

这些技能影响你「怎么思考和规划」：

- `/ideate` —— 需要多角度探索方向和创意时
- `/brainstorm` —— 不知道从哪里开始时
- `/spec` —— 需求模糊，需要先明确规格和验收标准时
- `/write-plan` —— 任务复杂，需要拆解时
- `/debug` —— 遇到 bug 或异常行为时
- `/lfg` —— 想全自动完成一个完整任务时

### 再调用实现类技能

这些技能影响你「怎么执行」：

- `/tdd` —— 需要编写代码时（测试先行）
- `/execute-plan` —— 有计划需要逐步执行时
- `/use-worktrees` —— 需要隔离开发环境时

### 最后调用收尾类技能

这些技能影响你「怎么完成」：

- `/verify` —— 实现完成，需要验证时
- `/multi-review` —— 需要多角度代码评审时
- `/request-review` —— 需要准备评审请求时
- `/git-commit` —— 需要提交代码时
- `/compound` —— 任务完成，需要沉淀经验时
- `/cso` —— 发布前安全审计时
- `/health` —— 需要代码质量评估时
- `/doc-release` —— 发布后同步文档时
- `/retro` —— 需要工程回顾时
- `/lint-lessons` —— 知识库需要体检时
- `/evolve` —— 搜索新的可吸收项目时
- `/finish-branch` —— 分支工作完成时

## 决策树

遇到任务时，按以下路径选择技能：

```
开始任务
  |
  +-- 需要探索方向？ ------------> /ideate
  |
  +-- 不知道从哪开始？ -----------> /brainstorm
  |
  +-- 想全自动完成？ ------------> /lfg
  |
  +-- 需求模糊要先明确规格？ ------> /spec
  |
  +-- 任务复杂需要拆解？ ---------> /write-plan -> /execute-plan
  |
  +-- 需要管理多个子任务？ -------> /todo
  |
  +-- 需要写代码？ --------------> /tdd
  |
  +-- 发现了 bug？ --------------> /debug
  |
  +-- 需要隔离环境？ ------------> /use-worktrees
  |
  +-- 实现完成了？ --------------> /verify -> /multi-review
  |
  +-- 需要提交代码？ ------------> /git-commit
  |
  +-- 想沉淀经验？ --------------> /compound
  |
  +-- 需要安全审计？ ------------> /cso
  |
  +-- 需要质量评估？ ------------> /health
  |
  +-- 发布后更新文档？ ----------> /doc-release
  |
  +-- 想做工程回顾？ ------------> /retro
  |
  +-- 知识库需要整理？ ----------> /lint-lessons
  |
  +-- 想搜索新的可吸收项目？ -----> /evolve
  |
  +-- 分支工作结束？ ------------> /finish-branch
  |
  +-- 多个独立子任务？ ----------> /dispatch-agents
  |
  +-- 需要多代理串行开发？ -------> /subagent-dev
  |
  +-- 收到评审反馈要处理？ -------> /receive-review
  |
  +-- 要执行危险命令？ ----------> /careful
  |
  +-- 想创建新技能？ ------------> /write-skill
```

## 组合使用示例

一个完整的功能开发流程：

1. `/ideate` —— 多角度探索方向
2. `/brainstorm` —— 理清需求和方案
3. `/write-plan` —— 制定实施计划
4. `/use-worktrees` —— 创建隔离开发环境
5. `/tdd` —— 测试驱动开发
6. `/execute-plan` —— 按计划逐步实现
7. `/verify` —— 运行 `{{test_command}}` 和 `{{check_command}}` 验证
8. `/multi-review` —— 多角度代码评审
9. `/compound` —— 沉淀经验到知识库
10. `/finish-branch` —— 合并或推送分支

> 或者直接运行 `/lfg` 自动串联上述全部步骤。

## 常见错误

- **"这个任务太简单，不需要技能"** —— 回顾 1% 法则。简单任务更容易因疏忽出错
- **"我已经知道怎么做了"** —— 技能不只是教你怎么做，还包含安全检查和质量保证
- **"先写代码再说"** —— 先调用流程类技能（brainstorm/plan），再写代码
- **"测试之后再补"** —— 使用 `/tdd`，测试永远先行
- **跳过收尾技能** —— `/verify` 和 `/finish-branch` 不是可选的

## 技能不存在时

如果决策树指向的技能还没有创建：
1. 告知用户该技能尚未创建
2. 建议使用 `/write-skill` 创建它
3. 在技能创建前，手动执行该技能本应覆盖的关键步骤
