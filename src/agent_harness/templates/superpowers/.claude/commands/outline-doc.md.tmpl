# 拟文档大纲（Outline-Doc）

铁律：**先列骨架，再填肉。**

写文档场景的"大纲先行"对应写代码场景的"测试先行"——没有骨架就直接展开段落，等于盲走。

---

## 何时使用

- 新建文档（标书 / 规范 / 白皮书 / 报告 / 技术方案）
- 重构现有文档（章节关系变了 → 重列骨架再搬内容）
- 章节重排（决定哪一章先讲、哪一章后讲）

不适用：
- 单页备忘录 / 一句话通知（直接写）
- 修字面 / 改 typo / 调格式（用编辑器即可）

---

## 输入要求

调用本 skill 前必须有：

1. **`/spec` 产出的规格文档**（含 R-ID 列表）
2. **目标读者画像**（标书评委 / 内部研发 / 外部客户 / 监管机构 等）
3. **目标字数下限**（用户指定或按文档类型推断；标书一般 ≥ 3000 字，规范一般 ≥ 1500 字）

缺任一项 → **🔴 停下来问用户**，不要凭直觉跳过。

---

## 5 步流程

### 1. 列章节标题

输出一级 + 二级标题树，每个节点标三件事：

- **字数估算**（如 `~500 字`）
- **R-ID 覆盖**（如 `R3, R5`）
- **难度**（`L=低 / M=中 / H=高`，写起来卡不卡）

示例：

```markdown
# 1. 项目背景  ~400 字  [R1]  L
## 1.1 行业现状  ~200 字  [R1]  L
## 1.2 客户痛点  ~200 字  [R1]  L

# 2. 技术方案  ~1200 字  [R2, R3, R4]  M
## 2.1 总体架构  ~400 字  [R2]  M
## 2.2 关键能力  ~500 字  [R3]  M
## 2.3 性能指标  ~300 字  [R4]  H
```

### 2. 每章一句话定位

每一级标题旁补一句"这章解决读者什么问题"。这一步逼你把章节关系想清楚——如果两章定位重复，合并；如果某章定位说不清，删掉。

```markdown
# 2. 技术方案
> 让评委信服：我们的方案能在 3 个月内交付且满足全部硬指标。
```

### 3. 标关键引用占位

凡涉及外部数据 / 文档 / 客户证言 → 插占位符 `[ref:source-name]` 或 `[data:metric-name]`。

```markdown
## 1.2 客户痛点
> 解决：让评委理解为什么这个项目值得做。
- [data:industry-revenue-loss-2025]
- [ref:client-survey-2026-q1]
- [ref:competitor-failure-cases]
```

**禁止**：用"据某某报告"、"某权威研究表明"这类模糊措辞——占位符不解析就**finalize 阶段过不了**。

### 4. 自检（机械可判）

写完大纲跑这 4 项 grep：

| 检查项 | 命令 | 通过条件 |
|---|---|---|
| 每条 R-ID 至少出现 1 次 | `grep -c "R<n>" outline.md` | ≥ 1 for each R-ID |
| 每章有定位句 | `grep -A1 "^# " outline.md \| grep "^>"` | 一级标题数 = `>` 行数 |
| 字数估算总和 ≥ 用户下限 | 手算或脚本求和 | 总和 ≥ 用户给的下限 |
| 无 TODO / TBD | `grep -E "TODO\|TBD\|待补充" outline.md` | 无输出 |

任一失败 → 修，不要带病往下走。

### 5. 产出 outline.md

写到任务工作目录（如标书项目的 `bid-2026-q2/outline.md`，或当前任务目录的 `outline.md`）。

---

## 完成标准

- [ ] 一级 + 二级章节都有字数估算 + R-ID 覆盖
- [ ] 每个一级章节有一句话定位
- [ ] 关键引用 / 数据全部用占位符标注
- [ ] 4 项自检全过
- [ ] outline.md 落盘，路径明确

---

## 反偷懒

| 借口 | 驳斥 |
|---|---|
| 「先写完再补大纲」 | 草稿期被错章节关系拖累的成本远大于先列大纲的成本——同样的字数往往要重写 2 遍 |
| 「凭直觉写就行，结构很清楚」 | 你觉得清楚 ≠ 读者觉得清楚。骨架是给读者的导航，不是给作者的备忘 |
| 「字数估算太麻烦，跳过吧」 | 没有字数预算就没有 plan 拆分依据；评审时也没办法判断"这章是不是写薄了" |
| 「占位符以后再加」 | finalize 阶段会硬卡占位符未解析；现在偷懒等于把成本推到最后一公里 |
| 「先复制旧文档大纲改改」 | 复制旧大纲容易把不适合本次的章节带过来——读者画像 / R-ID 都变了，结构也该变 |

---

## 一句话总结

**大纲不是写文档的可选准备，是文档的契约——读者按它判断你讲到没讲到，评审按它打分。**
