# 完成前验证

核心原则：**先有证据，再有结论。永远如此。**

当前项目：`{{project_name}}`（{{project_type}} / {{language}}）
测试命令：`{{test_command}}`
检查命令：`{{check_command}}`

## 五步验证门

在宣称任何工作完成之前，必须通过以下五步：

### 第 1 步：确定验证命令

根据要验证的内容，找到对应的命令：

| 验证内容 | 命令 |
|---|---|
| 测试通过 | `{{test_command}}` |
| 代码质量 | `{{check_command}}` |
| 构建成功 | `{{run_command}}` |
| 特定功能 | 根据场景确定 |

### 第 2 步：完整执行命令

- 运行命令，**不跳过任何输出**
- 不要用 `--quiet` 或 `--silent` 参数
- 让命令完整跑完，不要提前中断

### 第 3 步：阅读完整输出

- 从头到尾阅读命令输出
- 注意警告信息，不只是错误
- 注意跳过的测试或未覆盖的路径
- 看最终的汇总行（通过/失败/跳过的计数）

### 第 4 步：判断输出是否支持结论

对照验证标准：
- 测试：全部通过，无跳过，无回归
- 构建：无错误，无警告（或只有已知的警告）
- 代码检查：无新增违规

### 第 5 步：带证据报告结果

报告时必须包含：
- 运行了什么命令
- 关键输出（通过数/失败数）
- 结论

```
✓ 验证完成：运行 `{{test_command}}`，42 个测试全部通过，0 失败，0 跳过。
✗ 验证失败：运行 `{{test_command}}`，40 通过，2 失败。失败测试：test_xxx, test_yyy。
```

### 第 5.5 步：数量门禁（Anti-Laziness Gate #1）

验证清单中的**每一项**都必须有明确状态，禁止静默跳过。

完成验证后，输出汇总行：

```
数量门禁：已处理 N/N 项（✅ passed: X | ⚠️ skipped: Y | ❌ failed: Z）
```

**硬检查**：`X + Y + Z == N`。差值 > 0 说明有遗漏，必须补齐。

每个标记为 `⚠️ skipped` 的检查项，必须附带具体理由（引用文件路径或代码行），不能只写「不相关」或「不适用」——参见 `.claude/rules/anti-laziness.md` 门禁 3 反合理化表。

### 第 5.6 步：R-ID 覆盖硬检查（灵感自 GSD Nyquist 合规）

如果上游 `/spec` 产出过需求矩阵（R1, R2, ..., Rn），必须做**逐条 R-ID 覆盖硬检查**，禁止在任何 R-ID 悬空的情况下宣告完成：

```
## R-ID 覆盖核验

| R-ID | 需求描述 | 状态 | 证据 / 理由 |
|------|---------|------|-----------|
| R1 | 搜索输入框 | ✅ satisfied | test_search_input_renders |
| R2 | 200ms 返回 | ✅ satisfied | test_search_latency (95p=180ms) |
| R3 | 空结果提示 | ✅ satisfied | test_empty_state |
| R4 | 超长输入不崩溃 | ⚠️ out-of-scope | 与用户确认本迭代不做，记录在 follow-up Issue #XX |
```

**铁律**：
- 每个 R-ID 必须是以下三态之一：`✅ satisfied`（有测试/证据）、`⚠️ out-of-scope`（明确推迟 + 有理由）、`❌ missed`（遗漏）
- **出现任何 `❌ missed` → 验证失败**，回到 `/execute-plan` 补实施或和用户确认转为 out-of-scope
- **不允许**写"整体通过"而不逐条列 R-ID——这是 Scope Reduction 反模式
- 出现 `⚠️ out-of-scope` 必须注明在哪个后续 Issue 或 follow-up task 中追踪

### 第 6 步：实操穷举验证（关键功能必做）

如果改动涉及数据安全、用户内容、文件读写等关键路径，单元测试通过**不够**。必须写一个端到端验证脚本：

1. **一次性列出所有场景**——正常路径、边界情况、错误路径、冲突场景、组合场景，不要分批补充
2. 在临时目录中模拟真实使用流程
3. 每项检查输出 ✅/❌，末尾汇总
4. 全部通过才能继续

> 典型做法：写一个 Python 脚本，在 `/tmp/` 下创建测试项目，模拟用户操作，自动断言结果。

判断是否需要此步骤：
- 会不会丢用户数据？→ **必做**
- 会不会破坏用户文件？→ **必做**
- 只改了内部逻辑，对外无影响？→ 可跳过

## 绝对禁止

以下行为**没有例外**：

- **禁止** 说"应该能通过"而不运行命令
- **禁止** 说"测试通过了"而没有实际运行的输出
- **禁止** 只运行部分测试就宣称全部通过
- **禁止** 看到错误输出仍然说"完成"
- **禁止** 信任之前的运行结果——修改代码后必须重新验证

## 危险信号语言

如果你发现自己要说以下内容，**停下来去运行命令**：

- "这应该没问题"
- "看起来是对的"
- "大概率能通过"
- "我很有信心"
- "根据代码逻辑应该可以"
- "之前是通过的，改动很小"
- 在未验证时说"完成！"

这些都是**没有证据的猜测**。运行命令，拿到证据，然后再说。

## 反合理化

当你想跳过验证时，检查这张表。

| 你想说的 | 为什么不行 |
|---------|-----------|
| "这应该没问题，改动很小" | "应该"不是证据。运行命令只需要几秒，而带着 bug 进入下一阶段的修复成本是几十倍 |
| "我刚才已经验证过了" | 修改代码后必须重新验证。上次的结果已经过期了 |
| "只改了注释/文档，不影响功能" | 注释可能包含配置指令，文档可能包含可执行代码。运行测试确认 |
| "测试太慢了，跑一部分就够了" | 部分测试通过不代表全部通过。回归 bug 往往出现在你没跑的那部分 |
| "CI 会帮我验证" | CI 是最后一道防线，不是唯一的防线。本地验证能更快发现问题，也避免浪费 CI 资源 |
| "我很有信心这次没问题" | 信心不是证据。最有信心的时候恰恰是最容易忽视问题的时候 |

**铁律不变：先有证据，再有结论。永远如此。**

## 适用范围

所有需要确认结果的场景：

1. **测试**：`{{test_command}}` — 新功能、Bug 修复、重构后
2. **代码检查**：`{{check_command}}` — 每次提交前
3. **构建**：`{{run_command}}` — 修改构建配置后
4. **需求验证**：对照需求文档逐条确认
5. **代理工作**：其他 agent 完成的工作也要验证

## 验证失败时

1. 不要恐慌，不要急于修复
2. 完整阅读失败信息
3. 判断是新引入的问题还是既有问题
4. 如果是新问题 → 使用 `/debug` 流程
5. 修复后**重新运行完整验证**，不要只运行失败的用例

## 一句话总结

**运行命令 → 阅读输出 → 然后才能下结论。没有捷径。**
