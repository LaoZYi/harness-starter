# 系统化调试

核心原则：**没有根因分析，不做任何修复。**

当前项目：`{{project_name}}`（{{project_type}} / {{language}}）
测试命令：`{{test_command}}`
检查命令：`{{check_command}}`

## 阶段 1：根因调查

在提出任何修复方案之前，完成以下调查：

### 1.1 阅读错误

- 完整阅读错误信息和堆栈跟踪，不要只看第一行
- 识别错误发生的**精确位置**（文件、行号、函数）
- 区分：这是直接原因还是连锁反应？

### 1.2 复现问题

```
{{test_command}}
```

如果不能稳定复现，记录：
- 发生频率
- 触发条件
- 环境差异

**不能复现的 Bug 不要猜测修复。**

### 1.3 审查近期变更

- 查看最近的 commit，找到引入问题的变更
- 使用 `git log --oneline -20` 和 `git diff` 定位
- 如果能定位到具体 commit，问题已经缩小了一半

### 1.4 收集证据

在相关代码路径上添加日志或断点，观察：
- 输入数据的实际值（与预期值对比）
- 函数调用的实际顺序
- 状态变迁的每一步

### 1.5 追踪数据流

从数据入口到错误点，画出完整的数据流转路径：
- 数据从哪里来？
- 经过了哪些转换？
- 在哪一步变成了错误状态？

## 阶段 2：模式分析

### 2.1 找到相似的正常工作代码

项目中有没有类似功能是正常的？对比差异。

### 2.2 检查参考资料

- 框架文档中对这个 API 的说明
- 类似问题的 issue 或 changelog

### 2.3 识别差异

正常代码和出问题的代码之间，关键差异是什么？

## 阶段 3：假设与测试

### 3.1 形成假设

基于阶段 1-2 的证据，形成**一个**具体假设：

> "问题原因是 X，因为证据 A、B、C 指向这个结论。"

**不接受模糊假设**：
- 错误："可能是异步问题"
- 正确："Promise 链在第 3 步没有 await，导致数据库查询在未提交事务上执行"

### 3.2 最小化验证

用最小改动验证假设：
- 加一行日志
- 临时硬编码一个值
- 注释掉可疑代码段

如果假设被证伪 → 回到阶段 1，收集更多证据。

## 阶段 4：修复实现

### 4.1 先写失败测试

用测试复现 Bug：
- 测试描述 Bug 触发的条件
- 运行 `{{test_command}}` 确认测试失败
- 失败原因与 Bug 表现一致

### 4.2 应用修复

- **一次只修一个问题**
- 修复应针对根因，不是症状
- 修改范围尽可能小

### 4.3 全面验证

```
{{test_command}}
{{check_command}}
```

确认：
- [ ] Bug 复现测试通过
- [ ] 所有既有测试通过
- [ ] 没有引入新的警告或错误

## 关键停止点

遇到以下情况时**立即停下来**，重新评估：

- **猜测性修复**：你说"试试这个看看行不行" → 停，回到阶段 1
- **不理解数据流就提方案**：你还没追踪完数据流转 → 停，完成 1.5
- **同时修多个地方**：你在两个文件里同时改东西 → 停，一次只改一处
- **连续 3 次修复失败**：你的假设可能有根本性错误 → 质疑架构层面的理解是否正确

## 调试记录

调试过程中保持记录：

```markdown
## 调试记录：<问题简述>

### 症状
### 调查发现
### 假设 1：<描述> → 结果：证实/证伪
### 假设 2：...
### 根因
### 修复方案
```

复杂问题将记录写入 `.agent-harness/debug-log.md`。

## StuckDetector 接入

同一根因假设**连续 3 次被证伪** → 必须触发 task-lifecycle 规则中的 StuckDetector：立即停下、写卡点记录、运行 `/brainstorm` 重新发散假设空间，或输出 3 个候选方向给用户选。禁止在同一方向上继续投 tokens。详见 `.claude/rules/task-lifecycle.md` "卡死检测"一节。
