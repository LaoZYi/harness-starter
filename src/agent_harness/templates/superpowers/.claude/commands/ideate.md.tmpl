# 结构化构思：批量生成、严格筛选

在头脑风暴之前，先完成系统化的创意构思。核心原则：**大量生成 → 全部批判 → 只留幸存者。**

当前项目：`{{project_name}}`（{{project_type}} / {{language}}）

## 阶段 0：前置检查

开始之前：
- 搜索 `docs/superpowers/specs/` 下是否已有 `*-ideation.md`，避免重复工作
- 解读用户提示中的方向偏好（聚焦哪个领域？期望产出多少个想法？）
- 如果用户没有明确方向，默认覆盖全项目范围

## 阶段 1：上下文扫描

派遣 **4 个并行子代理** 分别扫描：

1. **项目文档代理**：阅读 README、CLAUDE.md、AGENTS.md、docs/ 下的文档，提取项目目标和约束
2. **结构扫描代理**：遍历目录结构，识别模块边界、依赖关系、代码规模分布
3. **历史经验代理**：阅读 `.agent-harness/lessons.md`，提取过去踩过的坑和已验证的模式
4. **演进趋势代理**：查看最近 30 条 commit，识别项目正在往哪个方向发展

每个子代理输出 **不超过 10 条** 关键发现。汇总后形成项目现状快照。

## 阶段 2：多框架构思

派遣 **4 个并行子代理**，每个使用不同的思维框架独立生成创意：

### 框架 1：用户痛点
> "什么最让用户沮丧？什么操作最繁琐？什么场景下用户会放弃？"

从用户视角出发，识别体验中的摩擦点。每个痛点对应一个改进想法。

### 框架 2：反转思维
> "什么会让这个项目彻底失败？" 列出失败因素，然后逐一反转为预防措施。

故意思考最坏情况，再把每个风险转化为改进机会。

### 框架 3：假设挑战
> "我们在做哪些隐含假设？如果这些假设是错的呢？"

质疑技术选型、架构模式、用户行为假设。每个被推翻的假设可能是一个突破口。

### 框架 4：杠杆点
> "改动最小但影响最大的地方在哪里？"

寻找系统中的高杠杆点——一行配置、一个抽象、一个约定的改变能带来连锁正效应。

每个子代理产出 **7-8 个** 原始候选想法，汇总约 30 个。

## 阶段 3：批判与筛选

对全部 ~30 个原始候选逐一评估。**没有想法可以跳过评审。**

### 评估维度（每项 1-5 分）

| 维度 | 问题 |
|------|------|
| 影响力 | 能改善多少用户的体验？改善幅度有多大？ |
| 可行性 | 以当前代码库和团队能力，实现难度如何？ |
| 风险 | 引入的复杂度和潜在副作用有多大？ |
| 紧迫性 | 不做会怎样？拖延的代价是什么？ |
| 一致性 | 与项目当前方向和架构哲学是否契合？ |

### 筛选规则

- 总分 < 12 → **淘汰**，记录淘汰理由
- 总分 12-17 → **待定**，需要更多论据
- 总分 > 17 → **进入幸存者名单**

最终保留 **5-7 个幸存者**。每个幸存者必须附带：
- 一句话描述
- 为什么它能通过筛选（具体引用评分）
- 预估实现规模（小 / 中 / 大）
- 最大风险点

## 阶段 4：输出

将幸存者写入：

```
docs/superpowers/specs/YYYY-MM-DD-<topic>-ideation.md
```

文档结构：

```markdown
# <主题> 创意构思报告

## 项目现状快照
## 幸存者排名

### 1. <想法名称>
- **描述**: ...
- **框架来源**: 用户痛点 / 反转思维 / 假设挑战 / 杠杆点
- **评分**: 影响力 X + 可行性 X + 风险 X + 紧迫性 X + 一致性 X = 总分
- **实现规模**: 小 / 中 / 大
- **最大风险**: ...

### 2. ...

## 被淘汰的想法（附理由摘要）
## 下一步
```

## 过渡到头脑风暴

输出完成后，提示用户：

> 构思报告已保存。选择一个幸存者想法，运行 `/brainstorm` 进入详细设计探索。

## 关键约束

- **不要只列想法不做批判** —— 未经评审的想法列表没有价值
- **不要跳过阶段 1** —— 脱离代码库的构思是空想
- **不要合并相似想法** —— 相似但不同的想法可能在筛选中有完全不同的命运
- 所有被淘汰的想法都要记录理由，证明筛选过程是严肃的
