# 架构决策记录（ADR）

在做出重要技术决策时，用结构化方式记录**为什么这么选**，而不只是记录选了什么。

当前项目：`{{project_name}}`（{{project_type}} / {{language}}）
测试命令：`{{test_command}}`

## 适用场景

以下情况**必须**创建 ADR：

- 技术选型（数据库、框架、消息队列、缓存方案等）
- 架构模式选择（微服务 vs 单体、REST vs GraphQL、同步 vs 异步等）
- 重大接口设计（API 版本策略、认证方案、数据格式等）
- 不可逆或高成本的技术决策（迁移方案、数据模型变更等）

例外情况（可跳过 ADR）：
- 纯实现细节（变量命名、函数拆分）
- 已有团队共识的惯例
- 可随时低成本撤回的选择

## 三种操作模式

### 模式 1：创建新 ADR

当面临架构决策时，按以下流程创建记录。

#### 1.1 识别决策

先明确这是不是一个"架构级"决策：

| 判断标准 | 是架构决策 | 不是架构决策 |
|----------|-----------|-------------|
| 影响范围 | 跨模块、跨团队 | 单个函数、单个文件 |
| 可逆性 | 高成本撤回 | 随时可改 |
| 约束周期 | 影响未来 3+ 个月 | 只影响当前任务 |
| 替代方案 | 有 2+ 个合理选项 | 显然只有一种做法 |

#### 1.2 用 MADR 模板记录

在 `docs/decisions/` 目录下创建文件，命名格式：`NNNN-<动词短语>.md`

```markdown
# NNNN. <决策标题>

## 状态

Proposed | Accepted | Deprecated | Superseded by [NNNN](NNNN-xxx.md)

## 背景

是什么问题或需求驱动了这个决策？

- 当前面临的具体问题
- 相关的技术约束和业务约束
- 利益相关方的需求

## 决策驱动因素

- 驱动因素 1（例：性能要求——P99 延迟 < 100ms）
- 驱动因素 2（例：团队熟悉度——3 人有 Redis 经验，0 人有 Memcached 经验）
- 驱动因素 3（例：运维成本——需要支持自动扩缩容）

## 备选方案

### 方案 A：<名称>

<简要描述>

- 优点：...
- 缺点：...

### 方案 B：<名称>

<简要描述>

- 优点：...
- 缺点：...

### 方案 C：<名称>（如有）

<简要描述>

- 优点：...
- 缺点：...

## 决策结果

选择**方案 X**，因为：

1. <最重要的理由>
2. <次要理由>
3. <次要理由>

### 正面后果

- <预期的好处 1>
- <预期的好处 2>

### 负面后果

- <已知的代价或风险 1>
- <已知的代价或风险 2>

### 需要关注的

- <后续需要验证的假设>
- <可能触发重新评估的条件>
```

#### 1.3 编号规则

扫描 `docs/decisions/` 目录，找到最大编号，+1 作为新编号。首个 ADR 编号为 `0001`。

```bash
ls docs/decisions/*.md 2>/dev/null | sort -r | head -1
```

#### 1.4 完成后

1. 将 ADR 状态设为 `Proposed`
2. 告知用户 ADR 已创建，位置和编号
3. 如果是在 `/lfg` 或 `/write-plan` 流程中，继续后续步骤

---

### 模式 2：查看现有 ADR

列出所有 ADR 及其状态，帮助理解历史决策。

```bash
# 列出所有 ADR
ls docs/decisions/*.md 2>/dev/null | sort
```

对每个 ADR：
1. 读取标题和状态行
2. 输出摘要表格：

```
## 现有架构决策记录

| 编号 | 标题 | 状态 |
|------|------|------|
| 0001 | 选择 PostgreSQL 作为主数据库 | Accepted |
| 0002 | 使用 REST 而非 GraphQL | Accepted |
| 0003 | 从单体迁移到微服务 | Proposed |
```

---

### 模式 3：更新 ADR 状态

当决策被推翻、废弃或替代时，更新原 ADR 状态。

#### 状态流转

```
Proposed → Accepted（决策被采纳并实施）
Accepted → Deprecated（决策不再适用，但无替代方案）
Accepted → Superseded by [NNNN](NNNN-xxx.md)（被新决策替代）
```

#### 替代流程

当创建新 ADR 替代旧 ADR 时：

1. 新 ADR 在背景中引用旧 ADR：`替代 [NNNN](NNNN-xxx.md)`
2. 旧 ADR 状态改为：`Superseded by [NNNN](NNNN-xxx.md)`
3. 旧 ADR 内容**不删除**，保留完整的决策演进链

## ADR 质量检查

每个 ADR 必须通过以下检查：

- [ ] **背景清晰**：读者不需要额外上下文就能理解问题
- [ ] **备选方案 ≥ 2**：至少评估了两个可行方案
- [ ] **优劣具体**：不是"性能好"而是"P99 延迟 < 100ms"
- [ ] **后果诚实**：负面后果不为空，每个决策都有代价
- [ ] **驱动因素可量化**：尽量用数字而非形容词

## 存储位置

ADR 文件存放在 `docs/decisions/` 目录下。

## 与其他技能的协作

- `/write-plan` — 计划中涉及架构选择时，先运行 `/adr` 记录决策
- `/compound` — 沉淀经验时检查是否有 ADR 需要更新状态
- `/brainstorm` — 设计对话产出的架构决策，用 ADR 正式记录
- `/lfg` — 阶段 0 读取相关 ADR，阶段 3 创建 ADR，阶段 9 更新状态

## 反合理化

| 你想说的 | 为什么不行 |
|---------|-----------|
| "团队都知道为什么选这个" | 半年后新人加入、你自己也会忘。口头共识不是文档 |
| "写 ADR 太慢了，先赶进度" | 5 分钟的记录能省 5 小时的考古。技术债中最贵的是决策债 |
| "这个决策太明显了，不需要记录" | 今天觉得明显，是因为上下文还在你脑中。上下文会随时间蒸发 |
| "备选方案只有一个" | 那不是决策，是约束。如果真的只有一条路，记录为什么没有其他选择 |
| "代码就是文档" | 代码告诉你选了什么，ADR 告诉你为什么选它、为什么没选别的 |
| "等稳定了再补" | 决策上下文的保质期极短。现在不记，两周后你就记不清当时的取舍了 |

## 一句话总结

**代码回答"是什么"，ADR 回答"为什么"。**
