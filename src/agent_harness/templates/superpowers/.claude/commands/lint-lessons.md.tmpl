# 知识库健康检查

对 `.agent-harness/lessons.md` 做全面体检，发现并修复知识库中的问题。

灵感来源：[Andrej Karpathy 的 LLM Wiki 模式](https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f) — 知识库需要定期 lint，就像代码需要定期 lint 一样。

当前项目：`{{project_name}}`（{{project_type}} / {{language}}）

## 适用时机

- 定期维护（建议每 1-2 周运行一次）
- lessons.md 条目超过 20 条时
- 感觉"上次好像记过类似的东西"时
- `/retro` 工程回顾后，顺手检查知识库健康度

## 检查流程

### 第 1 步：读取全部条目

读取 `.agent-harness/lessons.md` 的完整内容，解析每个条目的：
- 标题
- 分类
- 日期
- 症状、根因、解决方案、预防措施
- 相关文件

如果文件不存在或为空 → 告知用户，建议先通过 `/compound` 积累一些经验再来检查。

### 第 2 步：6 项健康检查

#### 2.1 重复检测

找出描述相同或高度相似问题的条目：
- 标题相似度高（表达不同但说的是同一件事）
- 根因相同但被记录了多次
- 解决方案实质相同

每组重复标记出来，建议合并为一条（保留最完整的版本，补充其他条目的独特信息）。

#### 2.2 矛盾 / 张力检测（灵感自 OpenSwarm 向量记忆 consolidation）

对所有条目做两两语义比对，找出「互相矛盾」或「存在张力」的候选对：

**矛盾的三种模式**：

1. **规则反向**：条目 A 说"X 应该这样做"，条目 B 说"X 不应该这样做"
2. **根因冲突**：同一问题在不同条目中给出了不同根因
3. **预防冲突**：两条预防措施互相排除（做了 A 就不能做 B）

**张力的两种模式**（不是硬矛盾，但值得审视）：

4. **适用边界模糊**：条目 A 和 B 对同一问题给出不同解法，没说明各自适用场景
5. **优先级未明**：多条教训都想在同一时机触发，但没说谁优先

**输出格式**：

```
### 矛盾 / 张力候选（共 N 对）

1. 🔴 矛盾 · 规则反向
   - A: "文件锁必须先锁再 truncate" (2026-04-12)
   - B: "文件写入前先 truncate 释放空间" (2026-03-01)
   - 建议裁决：A 新且针对并发场景，B 可能过时 → 更新或删除 B

2. ⚠️ 张力 · 适用边界模糊
   - A: "用 mock 隔离外部依赖"
   - B: "禁止 mock 替代真实数据库"
   - 建议裁决：两条都对，但缺适用条件 → 合并并明确"除数据库外可 mock"
```

**铁律**：
- 🔴 **只检出，不自动合并 / 删除**。人工决策不可省，因为矛盾可能反映了真实的条件分支，盲目合并会丢失重要区分。
- 每对候选必须给出裁决建议（4 选 1）：保留 A 删 B / 保留 B 删 A / 合并为一条 / 都保留但补充适用条件
- 裁决由用户在第 4 步确认后才执行

#### 2.3 过时检测

检查条目引用的文件和代码是否仍然存在：

```bash
# 对每个条目的"相关文件"字段
# 检查文件是否还存在
test -f <file_path>
```

- 引用的文件已被删除或重命名 → 标记为可能过时
- 引用的函数/类名在代码中已找不到 → 标记为可能过时
- 日期超过 6 个月且引用的代码已大幅变更 → 建议复审

#### 2.4 孤立检测

找出"没有价值"的条目：
- 根因写得太模糊（如"配置问题"、"代码 bug"）
- 解决方案不具体（如"修改了相关代码"）
- 缺少关键字段（无根因、无解决方案、无相关文件）

这些条目要么需要补充完善，要么应该删除。

#### 2.5 覆盖度分析

统计各分类的条目数量，识别盲区：

```
分类分布：
  构建错误：3 条
  测试失败：5 条
  运行时错误：2 条
  性能问题：0 条  ← 盲区
  数据库：4 条
  安全：0 条      ← 盲区
  UI：1 条
  集成：2 条
  逻辑错误：3 条
  架构决策：1 条
```

盲区不一定是问题（可能确实没遇到过），但值得留意。

#### 2.6 交叉引用检查

检查条目之间是否缺少关联：
- 相关的条目是否互相引用？
- 同一模块的多个问题是否指向一个更深层的系统性问题？

### 第 3 步：输出健康报告

```
## 知识库健康报告

文件：.agent-harness/lessons.md
条目总数：21
检查时间：YYYY-MM-DD

### 评分：7/10

| 检查项 | 结果 | 详情 |
|--------|------|------|
| 重复检测 | ⚠️ 发现 2 组 | 条目 #3 和 #15 描述相同问题 |
| 矛盾检测 | ✅ 无矛盾 | |
| 过时检测 | ⚠️ 发现 3 条 | 条目 #5、#8、#12 引用的文件已不存在 |
| 孤立检测 | ⚠️ 发现 1 条 | 条目 #7 缺少根因和解决方案 |
| 覆盖度 | ℹ️ 2 个盲区 | 性能问题、安全 分类无条目 |
| 交叉引用 | ✅ 正常 | |

### 建议操作

1. **合并** 条目 #3 和 #15（保留 #15，补充 #3 的信息）
2. **更新或删除** 条目 #5、#8、#12（引用文件已不存在）
3. **补充** 条目 #7 的根因和解决方案，或删除
```

### 第 4 步：自动修复（需确认）

对于可以安全修复的问题，提供一键修复选项：

| 操作 | 需要确认 |
|------|---------|
| 合并重复条目 | 🔴 展示合并后的内容，等用户确认 |
| 删除过时条目 | 🔴 展示要删除的内容，等用户确认 |
| 补充交叉引用 | ⚙️ 自动添加 |
| 更新文件路径 | ⚙️ 如果能找到新路径则自动更新 |

**铁律：绝不静默删除任何条目。** 删除必须经过用户确认。

## 健康评分标准

| 分数 | 含义 |
|------|------|
| 9-10 | 优秀 — 无重复、无矛盾、引用全部有效 |
| 7-8 | 良好 — 少量可修复的问题 |
| 5-6 | 一般 — 需要花时间整理 |
| 3-4 | 较差 — 大量重复或过时内容，知识库可信度下降 |
| 1-2 | 需要重建 — 建议清理后从头积累 |
