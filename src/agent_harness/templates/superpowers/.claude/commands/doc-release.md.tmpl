# 发布后文档同步

核心原则：**代码已发布，文档必须跟上。读完再改，改完再提交。**

当前项目：`{{project_name}}`（{{project_type}} / {{language}}）
运行命令：`{{run_command}}`
测试命令：`{{test_command}}`
检查命令：`{{check_command}}`

## 第 1 步：预检

确认当前分支状态和变更范围：

```bash
# 查看与基础分支的差异
git diff main...HEAD --stat

# 发现所有 .md 文件
find . -name "*.md" -not -path "./.git/*" -not -path "./node_modules/*"
```

将变更分为三类：
- **新增文件**：需要在文档中提及
- **修改文件**：可能导致文档过时
- **删除文件**：文档中的引用需要清理

如果没有代码变更（仅文档改动），告知用户并询问是否继续。

## 第 2 步：逐文件审计

对以下文档逐一检查，**必须先完整读取文件内容再做判断**：

### README.md
- [ ] 功能列表是否完整？新功能是否已列出？
- [ ] 安装指引是否准确？依赖是否有变化？
- [ ] 示例代码是否能正常运行？
- [ ] 故障排查部分是否覆盖新场景？

### docs/architecture.md
- [ ] 组件列表是否与代码结构一致？
- [ ] 设计决策说明是否反映最新架构？
- [ ] 数据流描述是否准确？

### CONTRIBUTING.md
- [ ] 开发环境搭建步骤是否可用？
- [ ] 测试流程是否准确？（验证：`{{test_command}}`）
- [ ] 工作流说明是否与当前实践一致？

### AGENTS.md / CLAUDE.md
- [ ] 项目结构描述是否与实际目录一致？
- [ ] 命令列表是否完整且准确？
- [ ] 规则和约束是否仍然适用？

### docs/product.md
- [ ] 功能清单是否完整？
- [ ] 新增功能是否有对应描述？

### docs/runbook.md
- [ ] 运行命令是否可用？（验证：`{{run_command}}`）
- [ ] 测试命令是否可用？（验证：`{{test_command}}`）
- [ ] 检查命令是否可用？（验证：`{{check_command}}`）
- [ ] 故障处理流程是否仍然有效？

如果某个文件不存在，跳过该文件的审计，不报错。

## 第 3 步：自动修正事实性错误

以下类型的错误**直接修正**，无需确认：
- 拼写错误和错别字
- 过时的命令引用（如旧的脚本名称）
- 错误的文件路径引用
- 过时的版本号引用
- 损坏的内部链接

每次修正记录修改内容，最后汇总展示。

## 第 4 步：标记叙事性变更

以下类型的变更**必须先向用户确认**，不得自行修改：
- 大段文字重写或删除
- 功能描述的措辞调整
- 安全相关内容的修改
- 弃用声明的添加或删除
- 架构决策描述的变更

将需要确认的变更列出，等待用户逐条批准或驳回。

## 第 5 步：CHANGELOG 润色

**铁律：绝不删除或替换已有的 CHANGELOG 条目。**

润色规则：
- 以用户视角撰写，用"你现在可以..."开头
- 好的例子："你现在可以通过 `--format json` 导出 JSON 格式的报告"
- 坏的例子："Added JSON export functionality"
- 保持时间顺序，最新的在最上面
- 只调整措辞，不改变语义

## 第 6 步：跨文档一致性检查

交叉验证以下内容，确保文档之间没有矛盾：

| 检查项 | 对比来源 |
|---|---|
| README 功能列表 | ↔ docs/product.md 功能列表 |
| architecture.md 组件 | ↔ 实际代码目录结构 |
| runbook.md 命令 | ↔ 实际可执行的命令 |
| CONTRIBUTING.md 流程 | ↔ 实际开发工作流 |
| CLAUDE.md 项目结构 | ↔ 实际目录树 |

发现不一致时，以**代码和实际行为**为准修正文档。

## 第 7 步：清理 TODO

### 已完成的 TODO
- 扫描文档中的 `TODO`、`FIXME`、`HACK` 标记
- 如果对应功能已实现，删除标记并更新描述

### 新增的 TODO
- 扫描代码中新增的 `TODO`、`FIXME`、`HACK` 注释
- 将重要的待办事项记录到合适的文档中

## 第 8 步：版本决策

询问用户是否需要版本号变更：
- 如果本次仅有文档变更，推荐 **SKIP**（不升版本）
- 如果包含功能变更，建议按语义化版本规则升级
- 等待用户明确回复后再操作

## 第 9 步：提交

如果有任何文档被修改：

```bash
# 逐文件暂存（禁止 git add -A，避免意外暂存敏感文件）
git add <被修改的文档文件路径1> <被修改的文档文件路径2> ...
git commit -m "docs: update project documentation"
```

如果没有任何修改，告知用户"文档已是最新状态，无需更新"。

## 任务日志更新

更新 `.agent-harness/task-log.md`：
- 记录本次同步了哪些文档
- 记录发现的问题和修正内容
- 记录用户确认的叙事性变更

如果 `.agent-harness/task-log.md` 不存在，创建该文件。

## 注意事项

- **先读后改**：修改任何文件前，必须完整读取其当前内容
- **CHANGELOG 只增不删**：已有条目神圣不可侵犯
- **不确定就问**：遇到模糊情况，向用户确认而非自行判断
- 所有路径相对于 `{{project_name}}` 项目根目录
- 保持文档风格一致：中文文档用中文标点，英文文档用英文标点
