---
description: 文档项目（标书 / 规范 / 白皮书 / 报告）的写作约定和定稿契约
paths:
  - "**/*.md"
  - "**/*.markdown"
  - "**/outline.md"
  - "**/draft.md"
---

# 文档项目规则

本项目是文档项目（{{project_type}}），主要产出 markdown 等可阅读文档而非可执行代码。

## 章节命名

- 一级标题用 `#`，二级用 `##`，三级用 `###`，**不跳级**（不允许 `#` 直接跳到 `###`）
- 标题用陈述性短语，不用问句、感叹句
- 同级标题命名风格一致（要么全是名词短语，要么全是动宾短语）

## 术语表

- 根目录维护 `glossary.md`，所有专业术语首次出现的定义都进这里
- 术语首次出现时必须有定义或脚注；后续可直接使用
- 缩写首次出现要展开：`AI（Artificial Intelligence）` → 后续可直接 `AI`
- 大小写归一：`HTTP` 不写 `http`、`Kubernetes` 不写 `k8s`（除非引用原文保留）
- 见项目 `CLAUDE.md` 的「术语大小写扩展清单」

## 引用格式

- 内部引用：`[ref:doc-name#section]`（指向同项目其它文档）
- 外部引用：`[ref:author-year]` + 文末参考文献段；或 `[url:link-id]` + 文末链接段
- 数据引用：`[data:metric-name]` + 文末数据来源段
- **草稿期可用占位**（`[ref:X]` `[data:Y]`），但**定稿期必须解析为真实出处**——`/finalize-doc` 检查 3 会硬卡

## 版本与日期

每个文档头部 frontmatter 含：

```yaml
---
title: <文档标题>
version: 1.0
date: YYYY-MM-DD
status: draft | review | final
authors: [<作者>]
---
```

- `status: draft` —— 草稿期，允许占位符存在
- `status: review` —— 已交评审，开始 P0/P1 修复
- `status: final` —— 通过 `/finalize-doc` 8 项必检后才能改为 final

## 变更记录

长文档（≥ 1000 字 或 ≥ 5 章节）末尾必须有 `## Changelog` 段：

```markdown
## Changelog

- v1.0 (2026-04-27): 首次发布；评委版
- v1.1 (2026-05-15): 修订术语统一；补 R6 章节
```

短文档（< 1000 字）可省略 changelog。

## 评审痕迹

- 评审反馈记录在 `.review/<doc-name>-<reviewer>-<timestamp>.md`，**不混入正文**
- 正文只保留最终内容，不留 P0/P1 修复痕迹（如 "// 评审建议：..."）
- 已 close 的 P2 推迟项进 `known-issues.md` 段，不留在文档主体

## 写作风格

- 默认遵循项目 `CLAUDE.md` 的「中文技术文档写作规范」
- 一段一个主要信息点，不连续堆叠 2 个以上长句
- 列表项要平行（统一命名结构 / 句式 / 信息密度）
- 标题反映用途，不只写抽象名词

## 不做的事

- **不**在文档项目里写可执行代码（除非是嵌入示例代码块）
- **不**用 `/lfg`（写代码流水线）；用 `/lfg-doc`
- **不**在 `/finalize-doc` 通过前发布对外
- **不**改 final 状态文档而不更新 version 和 changelog

## 一句话总结

**文档项目的产物是给读者读的，不是给编译器读的。每个约定都是为了让读者顺、评审清、维护稳。**
