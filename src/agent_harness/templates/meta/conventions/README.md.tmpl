# 跨服务约定 — {{project_name}}

所有微服务必须遵守以下约定。新增约定时在对应章节追加，并通知所有服务负责人。

## 错误响应格式

所有服务的错误响应必须统一为：

```json
{
  "error": {
    "code": "PAYMENT_NOT_FOUND",
    "message": "人类可读的错误描述",
    "details": {}
  }
}
```

- `code`：大写蛇形，前缀为服务域名（如 `PAYMENT_`、`ORDER_`）
- HTTP 状态码语义：400 客户端错误、404 资源不存在、409 冲突、500 服务端错误

## 认证与鉴权

- 服务间调用：（待补充，如 mTLS / JWT / API Key）
- 用户请求：（待补充，如 Bearer Token 格式）

## 日志格式

所有服务的结构化日志必须包含以下字段：

| 字段 | 说明 |
|------|------|
| `trace_id` | 分布式追踪 ID，跨服务传递 |
| `service` | 服务名，与 registry.yaml 中一致 |
| `level` | info / warn / error |
| `timestamp` | ISO 8601 UTC |
| `message` | 日志正文 |

## 接口版本策略

- （待补充，如 URL 前缀 /v1/、Header 版本号等）

## 超时与重试

- 服务间调用默认超时：（待补充）
- 重试策略：（待补充，如指数退避、最大重试次数）
