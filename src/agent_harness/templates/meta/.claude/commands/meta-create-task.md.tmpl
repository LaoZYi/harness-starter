# 创建跨服务任务

从会议纪要或需求描述中生成结构化的跨服务任务文件（半自动：AI 生成草稿 → 人工确认）。

## 使用方式

- `/meta-create-task` — 交互模式，粘贴会议纪要
- `/meta-create-task <文本>` — 直接从参数创建

参数：$ARGUMENTS

---

## 执行步骤

### 第 1 步：确认当前目录是 meta 仓库

检查 `services/registry.yaml` 是否存在。不存在则报错并停止。

### 第 2 步：获取输入

- 如果 `$ARGUMENTS` 非空，作为会议纪要/需求描述使用
- 如果为空，提示用户粘贴会议纪要或需求描述
- 输入过短（< 20 字）则追问补充

### 第 3 步：加载 meta 上下文

读取以下文件建立分析基础：

| 文件 | 提取什么 |
|------|---------|
| `services/registry.yaml` | 所有已注册的服务名、domain、负责人 |
| `services/dependency-graph.yaml` | 服务间的接口和依赖关系 |
| `business/domains/*/glossary.md` | 各领域的业务术语 |
| `business/domains/*/rules.md` | 各领域的业务规则 |
| `tasks/TASK-*.yaml` | 已有任务（避免重复、获取 ID 编号） |

### 第 4 步：分析并生成任务草稿

交叉分析会议纪要与 meta 上下文：

1. **识别涉及的服务** — 从纪要中提取服务名、功能模块或业务领域关键词，匹配到 registry 中的服务
2. **确定每个服务要做什么** — 根据纪要内容，为每个服务生成：
   - `goal`：一句话目标
   - `steps`：具体实施步骤
   - `tests`：需要验证的测试场景
   - `done_criteria`：完成标准
3. **推断依赖顺序** — 根据 dependency-graph.yaml 的接口关系和纪要中的逻辑，设置 `depends_on`
4. **生成任务上下文** — 从纪要中提取背景信息

### 第 5 步：生成任务 ID

扫描 `tasks/` 目录中的 `TASK-*.yaml` 文件：
- 提取现有最大编号，+1 作为新 ID
- 不存在则从 TASK-001 开始
- 零填充到 3 位（TASK-001, TASK-002, ...）

如果 `tasks/` 目录不存在，创建它。

### 第 6 步：展示草稿

以 YAML 格式展示完整任务草稿，重点标注：

- 识别出的服务和它们的 goal
- 依赖顺序（哪些可以并行，哪些需要等待）
- 纪要中提到但不在 registry 中的服务（警告：需要先注册）
- AI 做的推断和假设（哪些是纪要明确说的，哪些是推测的）

```yaml
id: TASK-NNN
title: 一句话标题
source: 来源描述
assignee: ""
status: draft
created: YYYY-MM-DD

context: |
  ...

repos:
  service-a:
    depends_on: []
    goal: ...
    steps: [...]
    tests: [...]
    done_criteria: [...]
  service-b:
    depends_on: [service-a]
    goal: ...
    steps: [...]
    tests: [...]
    done_criteria: [...]
```

然后询问：**"任务草稿是否准确？需要调整哪些内容？"**

### 第 7 步：写入任务文件

用户确认后：

1. 写入 `tasks/TASK-NNN.yaml`，status 为 `draft`
2. 询问：**"是否将状态改为 ready（可激活）？"**
3. 如果是 → 更新 status 为 `ready`
4. 输出下一步提示：`运行 /meta-activate-task TASK-NNN 激活任务`

---

## 注意事项

- 每条分析必须标注来源：是纪要原文还是 AI 推断
- 不在 registry 中的服务不要加入 repos，而是在展示时警告用户先注册
- 不要替用户填 assignee，留空让用户自己决定
- 如果纪要内容模糊，宁可留空让用户补充，不要硬猜
