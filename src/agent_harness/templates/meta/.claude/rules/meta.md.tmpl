---
description: Meta 仓库的注册表准确性和跨服务协调约束
paths:
  - "services/**"
  - "contracts/**"
  - "business/**"
  - "conventions/**"
  - "shared-plugins/**"
---

# Meta 仓库规则

本项目是 Meta 仓库（{{project_type}}），管理多服务体系的注册表、依赖关系、接口契约和共享规范。Meta 仓库不包含业务代码。

## 服务注册表准确性

- `services/registry.yaml` 必须与实际运行的服务一一对应。新增服务必须注册，下线服务必须标记 `status: deprecated` 并最终移除。
- 禁止出现「幽灵服务」：注册表中存在但实际已废弃的条目。每次变更注册表后，确认所有 `status: active` 的服务 repo 路径可达。
- 注册表字段（domain、owner、port）变更时，同步更新依赖图和相关契约文件。

## 依赖图完整性

- `services/dependency-graph.yaml` 中的 `provides` 和 `consumes` 必须与服务实际的接口调用一致。
- 禁止未声明依赖：服务 A 调用服务 B 的接口，但依赖图中没有记录。新增跨服务调用时，先更新依赖图再写代码。
- 检测循环依赖：A→B→C→A 的调用链必须在评审时识别并消除或显式标注为已知循环。

## 跨服务接口契约

- 接口定义必须双向一致：提供方声明的接口签名与消费方依赖的接口签名完全匹配。
- 接口变更流程：先在 meta 仓库更新契约 → 提供方实现 → 消费方适配。禁止提供方单方面改接口而不更新契约。
- 破坏性变更（删除字段、改变类型、移除端点）必须标注版本和迁移说明。

## 同步纪律

- Meta 仓库的变更必须在各服务仓库开始相关工作之前同步完成。禁止「先改服务代码，回头再补 meta」。
- 使用 `harness sync` 或 `/meta-sync` 将共享规范分发到各服务仓库。手动复制文件是禁止的，因为会导致版本漂移。
- 同步后验证目标仓库的规范文件与 meta 仓库一致。

## 业务知识组织

- `business/domains/` 按业务领域组织（支付、订单、用户等），每个领域一个目录。
- `business/products/` 按产品维度组织跨领域的业务描述。
- 领域划分变更（合并、拆分、重命名）必须同步更新注册表中服务的 `domain` 字段。

## 禁止业务代码

- Meta 仓库只包含元数据、契约、规范和脚本。禁止编写应用逻辑、API 实现或数据处理代码。
- `scripts/` 目录只放分发和校验脚本，不放业务脚本。
- 多服务需要共享的逻辑，提取到 `shared-plugins/` 作为规范模板，而不是在 meta 仓库中实现。
