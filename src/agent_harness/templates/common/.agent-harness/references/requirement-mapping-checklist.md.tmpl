# 需求 ↔ 测试 ↔ Plan Step 三元映射检查清单

L2 参考资料。灵感自 `gsd-build/get-shit-done` 的 Nyquist 合规理念 — 写任何代码前，每条需求必须有对应的自动化测试信号（或明确标 out-of-scope）。

当前项目：`{{project_name}}`（{{project_type}} / {{language}}）

## 核心理念

**每一条需求在整个开发生命周期中都是可追踪的**：

```
/spec 产出 R-ID → /write-plan 标注 task 实现哪些 R-ID → /verify 硬检查每个 R-ID 的 satisfied/missed 状态
```

违反这个链条的典型反模式：**Scope Reduction** — spec 里写了 R3，实施时默默跳过，验证时装作 R3 不存在。结果是需求分析和交付之间形成盲区。

## Spec 阶段（/spec）

- [ ] 每条验收标准分配了唯一 **R-ID**（R1, R2, ..., Rn 连续编号）
- [ ] 需求矩阵表存在且四列齐全：R-ID / 需求描述 / 验证方式 / 测试信号
- [ ] 「验证方式」具体到四类之一：自动化测试 / 性能测试 / 手动验证 / UI 截图
- [ ] 「测试信号」不能写"整体测试"——必须是具体测试名或可识别的验证动作
- [ ] 模糊需求（"更快" / "更好用"）已转化为可测量的标准

## Plan 阶段（/write-plan）

- [ ] 每个 task 的描述末尾标注了实现的 R-ID（如 `← 实现 R2, R5`）
- [ ] 计划末尾有「R-ID 覆盖表」：R-ID → task 编号的映射
- [ ] 每条 R-ID 至少映射到一个 task **或**明确标 `out-of-scope` 且给出理由
- [ ] 不存在"悬空 R-ID"（spec 里有但 plan 里既不实施也不标 out-of-scope）

## Verify 阶段（/verify）

- [ ] 生成了「R-ID 覆盖核验表」，逐条列出 R-ID
- [ ] 每条 R-ID 状态是以下三态之一：
  - ✅ **satisfied**（有测试 / 有证据）
  - ⚠️ **out-of-scope**（明确推迟 + 注明追踪 Issue）
  - ❌ **missed**（遗漏 → 必须回到实施或与用户确认转 out-of-scope）
- [ ] 没有 `❌ missed` 状态才允许宣告完成
- [ ] `⚠️ out-of-scope` 条目写清了"追踪 Issue #N" 或 "follow-up task"

## 反模式对照

| 看到这种描述 | 实际问题 |
|---|---|
| "全部验收标准通过" 但没列 R-ID | Scope Reduction — 可能漏了某条 |
| R-ID 从 R1 直接跳到 R4 | 编号不连续 — 有人偷偷删了需求 |
| 某 R-ID 的测试信号是"集成测试覆盖" | 信号不可识别 — 哪个测试？失败时怎么知道是哪条需求？ |
| plan 里某 task 没标 R-ID | 这个 task 是什么驱动的？不在 spec 里？还是 spec 漏了？ |
| `⚠️ out-of-scope` 没给 Issue 编号 | 推迟 ≠ 消失 — 没追踪等于丢失 |

## 与 /plan-check 的关系

`/plan-check` 会把上述检查项作为"需求覆盖维度"的校验标准。/plan-check 发现 `❌ missed` 或"悬空 R-ID"时会触发最多 3 轮修订循环，超过则升级到用户确认。

## 模糊需求 → 可验证目标转换模板

> 来源:Issue #51 吸收 forrestchang/andrej-karpathy-skills 的 Goal-Driven Execution 原则——"Strong success criteria let you loop independently. Weak criteria ('make it work') require constant clarification."

模糊需求(verb-only / 缺成功标准)→ 可验证目标(测试 / 命令 / 数字断言)的 7 类常见 pattern:

| 模糊形 | 可验证形(用作 R-ID 的验收标准) |
|---|---|
| "Add validation" | 写 N 条 test_invalid_<场景> 测试,跑 → 全部 fail → 实现 → 全部 pass |
| "Fix the bug" | 写 1 条复现 bug 的测试,跑 → fail → 实现修复 → pass + 不破坏现有 N 条相关测试 |
| "Refactor X" | 重构前后跑同一组测试 → 行为不变(diff 测试结果 = 0)+ 行数 / 圈复杂度 / coverage 至少不退步 |
| "Optimize Y" | 给出 before / after 量化指标(latency p99 / throughput / token 数 / 内存占用)+ 设阈值"提升 ≥ N%" |
| "Migrate from A to B" | 列出"还在用 A 的位置"清单(grep 命中数)→ 实施后 grep 命中数 = 0,旧 import / 旧 API 调用零残留 |
| "Improve UX" | 列具体场景:点击 X 后 Y 时间内见 Z 反馈(可手动验证或自动化)。无具体场景拒绝接 |
| "Support Z" | 写 demo 脚本调用新功能,exit 0 + 输出符合预期 schema(jq 校验) |

**判定原则**:每条 R-ID 必须能用**机械化命令**(测试 / grep / diff / curl)产出 ✅/❌ 二元结果。如果"通过"还需要人工解释(我觉得行 / 看起来不错),→ 不算可验证 → 退回 `/spec` 重新转换。

**写规格时**:把 R-ID 描述中的动词替换成上表右列的具体命令 + 阈值。

**反模式对照**:

- ❌ "提高用户体验" → 用上表第 6 行强制场景化
- ❌ "优化性能" → 用上表第 4 行强制量化指标
- ❌ "完善文档" → "完善"是动作不是结果,改成"文档中关键字段 X / Y / Z 都存在 + 长度 ≥ N 行"
- ❌ "重构以便扩展" → "以便扩展"是 YAGNI(详见 `.claude/rules/simplicity.md` 反合理化表),拒绝接
