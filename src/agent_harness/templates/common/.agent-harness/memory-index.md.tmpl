# Memory Index

> 热知识精华索引。详情见 `.agent-harness/lessons.md`、`.agent-harness/task-log.md` 和 `.agent-harness/references/`。
>
> **默认只读本文件**。需要深入某话题时，使用 `/recall <关键词>` 技能或直接 `grep` 完整文件。

## 最近教训（保留最多 10 条）

<!-- 写新教训时由 /compound 自动在顶部插入一行；超过 10 条时删除最老。 -->

（暂无，项目刚初始化。任务中积累教训后会自动填充。）

## 最近任务（保留最多 5 条）

<!-- 任务归档时自动在顶部插入一行；超过 5 条时删除最老。 -->

（暂无，首个任务完成后会自动填充。）

## 参考资料（.agent-harness/references/）

<!-- L2 温知识层。按需通过 `/recall --refs 关键词` 加载。-->

- `accessibility-checklist.md` — 无障碍检查清单（WCAG 2.1 AA）
- `performance-checklist.md` — 性能检查清单（Core Web Vitals）
- `security-checklist.md` — 安全检查清单（OWASP Top 10）
- `testing-patterns.md` — 测试模式参考（AAA、mocking、E2E）

## 主题索引（可选，按关键词定位历史）

<!-- 人工或 AI 周期性整理，为常见关键词建立指向 lessons.md 条目标题的索引。
     格式：`- 关键词 → lessons.md "条目标题"` -->

（尚未建立。当 lessons.md 条目较多时，可在此列出主题关键词方便定位。）

---

## 使用说明

- **task-lifecycle 规则**指示 AI 在开始新任务时默认读取本文件（而非 lessons.md / task-log.md 全量），避免上下文膨胀。
- 发现索引命中某话题 → 用 `/recall <关键词>` 技能或 `grep` 读取 lessons.md 对应节。
- references/ 为 L2 温知识（a11y / perf / security / testing）— 用 `/recall --refs` 按需查询。
- 索引由 `/compound` 技能维护；也可运行 `.agent-harness/bin/memory rebuild . --force` 从现有 lessons/task-log/references 重建一次（项目自带运行时，无需 harness CLI）。
