# Claude Code Notes — {{project_name}}

> 此文件在上下文压缩时不会丢失。最关键的约束放在这里，确保长对话中仍然生效。

## 项目快照

- 项目：{{project_name}}（{{project_type}}）
- 语言：{{language}} / {{package_manager}}
- 目标：{{project_summary}}
- 生产状态：{{production_status}}
- 敏感级别：{{sensitivity}}

## 关键命令

- 运行：`{{run_command}}`
- 测试：`{{test_command}}`
- 检查：`{{check_command}}`
- CI：`{{ci_command}}`

## 核心约束（与 AGENTS.md 同步，冗余放置以抵抗上下文压缩）

1. **禁止在任务结束时才批量更新文档**。每完成 1 个功能点就立即更新对应的 docs/ checkbox，因为会话中断会导致所有未记录的进度丢失。
2. **禁止跳过测试直接提交**。每次改动后运行 `{{test_command}}`，因为未测试的代码进入主分支后修复成本是即时修复的 5-10 倍。
3. **禁止执行不可逆操作**（rm -rf、git push --force、DROP TABLE），除非用户在当前对话中明确要求，因为这些操作无法撤销。
4. **禁止只把规则写在聊天记录里**。长期约束必须写回 `AGENTS.md` 或 `docs/`，因为聊天记录会被压缩丢失。
5. **出错必须归因**。返工时记录根因到 task-log、提炼教训到 lessons.md，因为不归因的错误会反复出现。

## 上下文预算

上下文窗口是有限资源，禁止无节制地读取文件：

- 读文件前先确认是否与当前任务相关，禁止"先全部读一遍再说"。
- 单次读取不超过 200 行。需要更多时用 offset 分段读取，只读相关部分。
- task-log.md 只读最近 5 条，禁止全部加载。lessons.md 全部读取（通常很短）。
- 禁止同时打开超过 5 个无关文件。打开新文件前，确认之前读的文件是否还需要。
- 搜索代码时优先用 grep/glob 定位，禁止逐个目录遍历。

## 开始任务前必读

1. `AGENTS.md` — 完整规则和工作流
2. `.agent-harness/current-task.md` — 如果有未完成任务，**从断点继续**，禁止重新开始
3. `docs/product.md` — 功能列表和完成标准的打勾状态
4. `.agent-harness/lessons.md` — 之前踩过的坑，避免再犯
5. `.agent-harness/task-log.md` 最近 5 条（不需要全部读完）

## Checkpoint 规则（防止长任务中断丢失进度）

1. 新任务开始时，将目标和计划写入 `.agent-harness/current-task.md`
2. 每完成一个步骤，立即更新 current-task.md 的进度
3. 每完成一个功能点，立即更新 `docs/product.md` 的对应 checkbox
4. 实现完成后标记为"待验证"，告诉用户可以验证。**禁止主动清空 current-task.md**
5. 用户确认通过后，才转移到 task-log.md 并清空 current-task.md
6. 用户验证不通过，在当前任务里继续修，不开新任务

## Harness 自维护规则

每完成一个功能点就立即更新对应文档（禁止堆到任务结束）：

- 新增/修改/移除功能 → 更新 `docs/product.md`
- 改了文件结构 → 更新 `docs/architecture.md`
- 改了运行方式 → 更新 `docs/runbook.md`
- 改了协作约束 → 更新 `AGENTS.md`
- 发现"待补充"占位符且已有足够信息 → 直接填充

## 交付验收规则

完成实现后，打开 `docs/product.md` 逐条对照。只有完成标准全部变为 `[x]` 才能告诉用户"已完成"。禁止留半成品。

## 错误归因规则

用户指出问题或自检发现不符时，必须：
1. 记录返工日志到 task-log.md（用户反馈、根因、修复、涉及文件）
2. 提炼教训到 lessons.md（错误、根因、规则）
3. 如因缺少约束导致，在 AGENTS.md 硬规则里新增一条

## Task Log 规则

任务完成后追加到 `.agent-harness/task-log.md`（格式：需求、做了什么、关键决策、改了哪些文件、通过的 checkbox）。超过 50 条时归档到 task-log-archive.md，只保留最近 20 条。

不要把新的长期规则只写在这里。
详细规则和工作流见 `AGENTS.md`。
