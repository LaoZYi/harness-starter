---
description: 架构反模式 — 硬编码字面/路径白名单在边界守卫场景是反模式,优先用自动发现/字段标记/通用模式
---

# 架构模式规则

本规则汇集本项目反复踩过的**架构反模式**,从 lessons 晋升而来。每条都附 ≥3 个真实案例。

## 反模式 1:边界守卫场景的硬编码白名单

### 核心原则

**当一个清单被用作"边界守卫"(决定允许 / 拒绝 / 触发 / 跳过)时,硬编码字面值或路径白名单是反模式。**

清单永远不全 → 漏项被静默放行(漂移) → 用户/AI 频繁触底添加新字面 → 维护成本指数上升 → 反噬用户体验。

### 适用 vs 不适用判断

**反模式触发条件(任一即是)**:

- 清单内容来自**外部行为**(AI 写什么状态字、用户写什么文档名、模板新增什么模块)
- 清单的"完整性"不能机械证明(没有"所有可能值"的有限集)
- 漏项时**静默退化**为不安全 / 不放行 / 不检查(漂移是隐形的)

**不是反模式(正向枚举,继续用清单 OK)**:

- 清单是**协议规范**的有限集(HTTP 状态码、KNOWN_TYPES 事件类型)
- 清单是**自身定义**的封闭集(应用支持的 capability 角色名)
- 漏项时**显式报错**(不接受未注册类型)

### 推荐替代方案(按优先级)

| # | 方案 | 适用场景 | 案例 |
|---|---|---|---|
| 1 | **自动发现** (`rglob` / `walk` / 反射) | 文件系统/模块扫描 | 2026-04-13 守卫禁用白名单改自动发现(check_repo `rglob`) |
| 2 | **结构性字段标记** (存在性 + 非空校验) | AI 行为识别 | 2026-04-25 stop hook 改 `^##\s*状态[:：]<非空>` |
| 3 | **策略统一化** (对所有该类对象一视同仁) | 用户数据保护 | 2026-04-21 upgrade three_way 不维护"用户文档白名单",所有 three_way 文件统一保护 |
| 4 | **存在 + 非空 双门** (替代字面清单) | 字段值检查 | 同 2:`字段存在 && 字段值非空` 比 `字段值 ∈ {A,B,C}` 更鲁棒 |

只有上述方案**全部不适用**时,才接受白名单——并必须配契约测试锁死"白名单未漂移"(对照真实集合 / 自动发现结果)。

### 决策树

```
发现自己想加 ALL_CAPS_WHITELIST = [...]
  │
  ├─ 是边界守卫吗?(决定允许/拒绝/触发/跳过) ── 否 → 正向枚举 OK
  │
  ├─ 内容来自外部行为?(AI/用户/模板新增) ── 否 → 协议固定集 OK
  │
  ├─ 漏项静默放行?(没有显式报错) ── 否 → 显式报错 OK
  │
  └─ 三问全是 → 反模式
       ↓
  按优先级试:1.自动发现 → 2.字段标记 → 3.策略统一化 → 4.存在+非空双门
       ↓
  全部不行 → 白名单 + 契约测试锁死(必须证明永不漂移)
```

### 历史案例(本规则源自这 5 次反复触发)

1. **2026-04-13 [工具脚本] 守卫禁用白名单改自动发现** — `check_repo.py:check_module_sizes` 9 模块白名单漏新增 `squad/` 子包 → 改 `rglob` 自动发现
2. **2026-04-14 [工具脚本] _runtime 模块清单是 dogfood 的一部分** — `_RUNTIME_MODULES` 显式白名单忘加 `memory_search.py` → 内嵌运行时崩(此条尚未根治,仍靠契约测试兜底)
3. **2026-04-21 [架构设计] 策略默认值在边界场景必须显式处理不得静默退化** — 不维护"用户文档白名单",对所有 `three_way` 文件统一保护(策略统一化)
4. **2026-04-23 [(已晋升)] stop hook 5 字面状态白名单** — 1 → 5 字面扩展,仍不够穷举
5. **2026-04-25 [架构设计] 守卫机制依赖严格字面白名单时 AI 遵循率不足,应改通用字段标记** — stop hook 改通用 `## 状态[:：]<非空>` 字段标记

### 反合理化(配合 anti-laziness 门禁 3)

| 借口 | 驳斥 |
|------|------|
| 「反正只是再加一个字面/路径就行」 | 每次加一个的代价感觉小,但**漂移是单调累积的**——5 次累积说明白名单根本不该存在。改自动发现/字段标记一次到位,比 N 次 patch 总成本低 |
| 「正向枚举和反模式都能用,差不多」 | 不一样。正向枚举漏项**显式报错**(立刻发现),反模式漏项**静默放行**(漂移到漏出 bug 才发现) |
| 「KNOWN_TYPES 也是清单啊,凭什么这个不行」 | KNOWN_TYPES 是协议规范(自身定义的封闭集 + 漏项报错);本反模式针对"外部行为驱动 + 漏项静默"的场景。看决策树三问 |
