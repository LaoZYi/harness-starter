---
description: 测试编写和执行约束
paths:
  - "**/tests/**"
  - "**/test_*"
  - "**/*_test.*"
  - "**/*.test.*"
  - "**/*.spec.*"
---

# 测试规则

## 测试场景设计（写在 current-task.md 里，写代码之前）

每个功能必须覆盖三类场景，缺任何一类都不算完整：

| 类别 | 覆盖什么 | 举例 |
|------|---------|------|
| **正常路径** | 核心功能按预期工作 | 合法输入返回正确结果、CRUD 各操作正常 |
| **边界情况** | 极端但合法的输入 | 空字符串、零值、最大长度、空列表、单元素、并发调用 |
| **错误路径** | 非法输入和异常情况 | 参数类型错误、权限不足、依赖服务超时、数据不存在 |

设计原则：
- 从用户视角出发，想"用户会怎么用、怎么误用"
- 每个完成标准至少对应一个测试用例
- 如果改动涉及已有功能，补充回归测试确认没破坏

## 测试执行顺序

1. **先写测试**，运行确认全部失败（证明测试有效，不是空壳）
2. **再写实现**，逐个让测试通过
3. **全部通过后**运行 `{{test_command}}` 确认无回归

## 浏览器验证（适用于有 UI 的项目）

代码测试通过后，如果改动涉及用户可见的界面或交互，必须用浏览器工具做一轮真实验证：

1. 确认 dev server 已启动（`{{run_command}}`）
2. `navigate_page` → 打开改动涉及的页面
3. `take_snapshot` → 获取页面元素树，确认目标元素存在（按钮、输入框、文字等）
4. 操作交互：`fill` 填写表单、`click` 点击按钮、`press_key` 键盘操作
5. `wait_for` → 等待预期结果出现（成功提示、页面跳转、数据更新等）
6. `take_snapshot` → 确认页面状态符合预期
7. `take_screenshot` → 截图留证，附在验证说明中给用户

验证要点：
- 正常操作流程走通
- 空输入、非法输入时有合理提示（不是白屏或报错）
- 如有加载状态，确认 loading → 数据展示的过渡正常
- 如涉及表单，确认提交后数据持久化（刷新页面不丢失）

如果项目不涉及 UI（纯后端、CLI、库），跳过此节。

## 实操穷举验证（重要功能必做）

单元测试通过后，如果改动涉及**数据安全、用户内容、文件读写、升级迁移**等关键路径，必须用真实环境做一轮端到端穷举验证。

**执行方式**：写一个一次性脚本，在临时目录中模拟真实使用场景，自动检查所有预期结果。

**必须覆盖的五类场景**（一次性全列出，不要分多次补充）：

| 类别 | 必测内容 |
|------|---------|
| **正常路径** | 核心流程走通、各种合法输入、连续多次操作 |
| **边界情况** | 空输入、超大输入、特殊字符（emoji/多语言/代码块）、单行/单字段 |
| **错误路径** | 输入损坏、依赖缺失、文件权限问题、格式错误 |
| **冲突/竞争** | 双方修改同一位置、中间状态被破坏、迁移路径 |
| **组合场景** | 多种类型同时出现、连续操作、dry_run 不改实际文件 |

**输出格式**：每项检查一行 ✅/❌，末尾汇总通过/失败数。禁止"目测看着没问题"代替自动化检查。

**时机**：在告知用户"完成"之前。不是可选步骤。

## 测试质量约束

- 禁止用 mock 替代真实的数据库或外部服务测试，除非真实环境确实不可用。因为 mock 和真实行为的差异会掩盖集成 bug。
- 每个测试必须独立运行，禁止依赖其他测试的执行顺序或副作用。因为并行执行和单独重跑时会失败。
- 禁止在测试中硬编码时间戳、随机数或环境相关的值。使用固定种子或注入机制，因为不确定性会导致间歇性失败。
- 测试失败后，先读错误信息定位根因，禁止盲目重试相同命令超过 2 次。如果 2 次后仍失败，停下来分析原因。
- 修改业务逻辑后，必须运行 `{{test_command}}` 确认无回归。禁止跳过测试直接提交。
