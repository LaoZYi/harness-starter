---
description: 知识冲突解析——5 型分类 + 标准处理路径，给 lessons.md 维护链路一致的裁决准则
---

# 知识冲突解析（Knowledge Conflict Resolution）

> **来源**：吸收自 [ilang-ai/Imprint](https://github.com/ilang-ai/Imprint) v2.1 的 5 型 Conflict Resolution。
> **适用**：本项目 `lessons.md` 维护链路（`/lint-lessons` 和 `/compound`）。
> **分类标记**：`collaborative-permanent`——即使模型对齐改善，知识库一致性规则依然需要。

## 为什么有这条规则

`lessons.md` 会随任务推进积累矛盾条目。**单纯发现矛盾不够**，还得告诉消费者「该怎么办」。本规则把冲突按**解决路径**（不是症状）分成 5 型，每型有标准处理动作。

与 `/lint-lessons` 原有的 5 种症状分类（规则反向 / 根因冲突 / 预防冲突 / 适用边界 / 优先级）**正交、互补、叠加**——症状维度回答「是什么」，解决维度回答「怎么办」。

## 6 型速查表

| 型 | 场景 | 判定信号 | 标准处理路径 |
|---|---|---|---|
| **T1** | 用户本轮显式指令 vs 历史行为规则 | 用户说「这次要 X」但 lessons / rules 说「通常 Y」 | 跟用户本会话，不改 rule；若连续 3 次以上用户同一指令 → 升级成新规则 |
| **T2** | 全局规则 vs 项目 / 团队规则 | `.claude/rules/` 默认规则与 `.harness-plugins/rules/` 或项目 CLAUDE.md 冲突 | 项目 / 团队规则覆盖全局，记录 mismatch，不删全局条目 |
| **T3** | 两条 confirmed lesson 互相矛盾 | 两条都高置信度、都在 lessons.md、给出相反建议 | 转条件分支（`when:`）+ 记录 mismatch，**不删任一** |
| **T4** | 多 agent 给出不一致结论 | `harness agent aggregate` 或 squad reviewer 输出里出现冲突推断 | 两条都**降级为 tentative**，等用户确认后才固化 |
| **T5** | lesson 与当前任务冲突 | lesson 说"不要做 X"但本次任务主动要做 X | lesson 是**警告不是阻断**；自然提醒风险，不拒绝任务 |
| **T6** | lesson 反复触发，自然语言约束管不住 | 同一 lesson 被触发 ≥ 3 次 / 对应规则在 anti-rationalization 表里被反复拿借口绕过 | **晋升**：尝试脚本化为门禁，或升级为更强的 Rule 条目；不做到就一直在同一个坑上栽 |

## 在本项目 lessons 域的适用性

**本次接入 T3 / T4 / T5 / T6**到 `/lint-lessons` 和 `/compound` 的流程。

- **T1** 不接入（out-of-scope for lessons）：属于 AI 行为准则范畴，不该作为 lesson 维护问题处理。T1 出现时走 `.claude/rules/` 和 CLAUDE.md 的用户指令优先级规则
- **T2** 不接入（out-of-scope for lessons）：属于规则层级问题，不该作为 lesson 维护问题处理。未来吸收「用户级跨项目 profile」（Imprint 点 3）或多规则层级协调时可复用此型的锚点

**保留 T1 / T2 描述**是为未来扩展留语义位置——看到"T1/T2"时就知道它属于哪一类，避免新机制吸收时重复造轮子。

## T3 / T4 / T5 的详细处理路径

### T3：两条 confirmed lesson 相互矛盾

**场景示例**：
- A（2026-03-15，confirmed）："测试里用 `mock` 隔离外部依赖，避免抖动"
- B（2026-04-20，confirmed）："测试禁止 `mock` 替代真实数据库，因为会掩盖迁移 bug"

**处理路径**：
1. **不删任一条** —— 两条可能都对，只是缺适用条件
2. **转条件分支**：合并为一条，明确 `when:` 语义。例：
   > 测试依赖隔离：
   > - `when:external_service` → 使用 `mock`（A 的适用场景）
   > - `when:database or storage` → 使用真实环境（B 的适用场景）
3. **记录 mismatch**：在合并后的条目里标注"原 A、B 合并"，保留可追溯性
4. **交由用户确认**：`/lint-lessons` 输出建议，`/compound` 在遇到 T3 时做预检提示，都**不自动执行合并**

#### T3 的 4 决策 SOP（dedup decision，OpenViking 吸收）

> 来源：volcengine/OpenViking 在记忆抽取时的「向量预过滤 + LLM 4 选 1」机制（参考 `docs/en/concepts/06-extraction.md`）。本项目复用现有 `.agent-harness/bin/memory search` 的 BM25 做相似度预过滤，**不引入** embedding / vecdb / faiss 依赖，**不拷贝** OpenViking 代码（AGPL-3.0）。

把上面 4 步「处理路径」从模糊建议升级为**可操作 SOP**：

**前置（相似度预过滤）**：用 BM25 拿 top-3 相似条目（不引入 embedding 依赖）。

```bash
.agent-harness/bin/memory search "<新 lesson 关键词>" --top 3 --scope lessons
```

**4 决策（基于根因方向、解决方案、适用边界做 4 选 1）**：

| 决策 | 触发信号 | 标准动作 |
|---|---|---|
| `skip` | 新 lesson 与某条 confirmed lesson 语义高度重合（根因一致 + 解决方案相同） | **不写新条**；在 `current-task.md` 记一次"引用 <旧条目> 即可" |
| `create` | 新 lesson 是真正全新信息（新根因 / 新解决方案），与已有条目相关度低 | **正常追加**新条目（按 `/compound` 第 4 步） |
| `merge` | 新 lesson 与某条已有条目部分重合，但各自有独立 `when:` 适用条件 | **合并为一条**带 `when:` 条件分支；保留双方原始信息在正文 |
| `delete` | 新 lesson 证伪了某条 confirmed lesson（旧条在新环境下已无效） | 旧条**不物理删**，标注 `⚠️ deprecated by YYYY-MM-DD <新标题>` 归档；新条按 `/compound` 第 4 步写入 |

**硬约束（与 T3 原处理路径并存，不互相替代）**：

- 🔴 **不自动执行决策**。AI 输出 4 决策建议给用户确认后才动手——延续 T3 原 4 步的「不自动执行」铁律
- 🔴 **`merge` 和 `delete` 必须同步刷新** `.agent-harness/memory-index.md`，避免 L1 索引过时指向已合并/归档条目
- 🔴 **`delete` 不物理删**：旧条保留在 `lessons.md` 加 deprecated 标注作追溯锚点（与 T6 晋升「原 lesson 不删」同理，保留历史可回溯）
- 🔴 **决策结果是元分类，不替代「条件分支」语义**：`merge` 决策落地仍按 T3 原步骤 2「转条件分支」执行——4 决策回答「该不该合 + 如何写入」，原 4 步回答「合并的具体形态（when: 语义）」

### T4：多 agent 结论不一致

**场景示例**：
- squad 里 scout-a 的 artifact 说"项目栈是 React 18"
- 同 squad 里 scout-b 的 artifact 说"项目栈是 Vue 3"
- 两条 artifact 都被汇总进 diary，下游 builder 要用

**处理路径**：
1. **两条都降级为 tentative**：在 lessons 或 artifact 记录上打 `confidence:tentative` 标签（本项目未引入 confidence 字段时可用文字标注"待确认"）
2. **不擅自二选一**：即使某一方看起来更可能正确，也不自行裁决
3. **等用户确认**：显式向用户展示两条冲突结论 + 上下文，由用户选
4. **确认后才固化**：用户选定后升级为 confirmed，另一条归档到历史记录但不删除

### T5：lesson 与当前任务冲突

**场景示例**：
- lesson：「serverless 无共享 state，不要依赖进程内缓存」（confirmed）
- 当前任务：明知风险仍要做 serverless + in-memory cache demo

**处理路径**：
1. **lesson 是警告不是阻断**：AI 不拒绝任务、不跳过实施
2. **自然提醒风险**：在计划或实施开头用 1-2 句话提及 lesson 内容及其风险场景
3. **不重复提醒**：只在任务开始时说一次，不每步 remind
4. **不记录为 rework**：T5 场景下任务正常执行，不触发 `error-attribution` 流程

### T6：脚本化候选——反复触发的 lesson 应该"升级"

> 来源：腾讯 JK Launcher 团队《Harness Engineering 工程化落地》的核心观察——「Rule 再多，本质上还是自然语言约束。Rule 只能做原则约束，不能做流程执行。能判定的，就尽量不要停留在 Rule 层，而应该下沉成脚本」。作者同样明确：错题集（lessons）如果只长期停在记忆层，团队对不齐、新人接不上、事后也说不清依据，**真重要的就该晋升级**。

**场景示例**：
- lesson：「改模板后必须跑对应的同步命令」，已经被触发第 3 次（每次都是改模板没同步，被 verify 抓到）
- 现状：lesson 写了，但 AI 每次都拿「这次改动很小」「dogfood 应该不影响」绕过
- 信号：再写第 4 条同款 lesson 没意义——问题不在"知道"，问题在"做"

**处理路径**：
1. **触发条件（任一满足即考虑 T6）**：
   - 同一 lesson（或语义等价的 lesson）被触发 ≥ 3 次
   - lesson 对应的场景在 `anti-laziness.md` 门禁 3 反合理化表里反复被拿借口绕过
   - lesson 写的是**可机械判定**的内容（是否跑了某命令、是否出现某字符、文件是否存在），却只靠自然语言约束
2. **评估晋升路径**（按优先级）：
   - **A. 脚本化为硬门禁**（最优）：写进 `/verify` 检查项 / 新建 git hook / 加入 CI step — 让"是否合规"从 AI 解释变成脚本判断
   - **B. 升级为更强的 Rule 条目**：从 lessons.md 挪到 `.claude/rules/<domain>.md`，并在 `AGENTS.md` 或 CLAUDE.md 里引用 — 提升优先级和加载位置
   - **C. 加入 anti-laziness 反合理化表**：如果问题是"AI 拿借口绕"，就把借口 + 驳斥明写进表里
3. **晋升后**：原 lesson 条目**不删**，但标注「已晋升 → `<脚本路径>` / `<Rule 路径>`」，作为追溯记录
4. **不自动执行**：和 T3/T4 一样，`/lint-lessons` 只输出"建议晋升"提示，最终由用户决定走哪条路径（A/B/C 都可能不合适，比如有些 lesson 本质就是经验而非可判定规则）

**反模式**：
- ❌ 第 N 次踩同一个坑，还在写新 lesson 而不考虑 T6 晋升 — 无效沉淀
- ❌ 把所有 lesson 都一刀切脚本化 — 经验性 lesson（"涉及 A 模块要先问 B 同学"）本来就不是可判定的，强行脚本化只会产生废检查
- ❌ 晋升后删原 lesson — 丢失"为什么会有这条规则"的历史锚点

## 与 `/lint-lessons` 的接入点

`/lint-lessons` 步骤 2.2 发现矛盾 / 张力对后，除了输出原有的症状分类，**额外**标注 resolution-type。同步新增**步骤 2.3 脚本化候选扫描**：对每条 confirmed lesson 统计 task-log 里被触发的次数（文本匹配或 tag 对齐），触发 ≥ 3 次的自动标 `resolution-type: T6`。

```
### 矛盾 / 张力候选（共 N 对）

1. 🔴 矛盾 · 规则反向 · resolution-type: T3
   - A: "测试用 mock 隔离依赖" (2026-03-15)
   - B: "测试禁止 mock 替代数据库" (2026-04-20)
   - 建议动作：转条件分支（when:external_service vs when:database），合并为一条

2. ⚠️ 张力 · 适用边界模糊 · resolution-type: T3
   - A: ...
   - B: ...
   - 建议动作：合并并明确 when: 适用条件

3. 🔴 矛盾 · 根因冲突 · resolution-type: T4（由 squad aggregate 产出）
   - A: scout-a artifact
   - B: scout-b artifact
   - 建议动作：两条均降级 tentative，人工选定后固化

### 晋升候选（T6，共 M 条）

1. ⚠️ 「改模板后必须跑对应同步命令」—— 已触发 4 次
   - 判定：可机械判定（git diff + 命令存在性）→ 推荐路径 A（脚本化）
   - 建议动作：在 /verify 新增一条「模板变更 → 要求同步命令的产物时间戳新于模板 mtime」
2. ⚠️ 「PR 描述必须列影响模块」—— 已触发 3 次
   - 判定：可机械判定（正则扫 PR body）→ 推荐路径 A
   - 建议动作：加入 CI 的 PR lint step
```

当冲突不属于 T3/T4/T5/T6 任何一型（例如属于 T1 用户指令或 T2 规则层级问题）时，resolution-type 字段标 `N/A`，并在建议里说明"此类冲突不适合在 lessons 域处理"。

## 与 `/compound` 的接入点

`/compound` 步骤 3（查重与合并）在现有"重叠度判断"之后，新增步骤 3.5 做冲突预检：

1. 若检测到**新 lesson 与已有条目不是重叠而是相反**（根因不同但指向同一问题 / 预防措施互斥）
2. 按 T3 / T4 / T5 / T6 分型：
   - 如果是用户本次任务踩过的坑总结，和已有 lesson 方向相反 → **T3**
   - 如果来自 squad / `harness agent aggregate` 的多 agent 产物 → **T4**
   - 如果新 lesson 只是警告本次任务的潜在风险，不是总结本次实际教训 → **T5**（一般不需要写新 lesson，直接引用旧的）
   - **如果新 lesson 与已有条目语义高度重合，且已有条目已被触发 ≥ 2 次（算上本次就 ≥ 3）→ T6**：别再写同款，考虑晋升
3. 提示用户：
   - T3 → 推荐"转条件分支合并"而非新建
   - T4 → 推荐"两条都写 tentative"
   - T5 → 推荐"别写新 lesson，只在 current-task 里记一次风险提醒"
   - **T6 → 推荐晋升路径 A/B/C（脚本化 / 升级 Rule / 写入反合理化表），而非追加第 N 条同款 lesson**
4. **不 block 写入**：用户如仍坚持写入，`/compound` 正常执行，只是 T3 / T6 情况下会在日志里留一条 mismatch / 晋升候选备忘

**步骤 3.6（dedup decision）**：步骤 3.5 完成冲突分型后，`/compound` 进一步对新 lesson 和 `memory search --top 3 --scope lessons` 返回的 top-K 相似条目做 **4 选 1 决策**（skip / create / merge / delete，见上文「T3 的 4 决策 SOP」）。3.5 回答"是不是矛盾、是哪一型"；3.6 回答"具体该怎么处置"。两步叠加，不互相替代。

## 铁律

- **不自动合并 / 删除 / 降级 / 晋升**：所有处理路径都必须经用户确认
- **不把 T1 / T2 当 lessons 问题处理**：它们不属于本规则的执行范围
- **叠加使用症状维度和解决维度**：两者都要输出，不互替代
- **T6 不代表"必须脚本化"**：只代表"值得评估晋升"。经验性 lesson 可以永远停在 lessons 层，关键是反复触发时要**显式**做一次决策，而不是默认继续写同款
- **T3 冲突必须叠加 dedup decision**：`/lint-lessons` 和 `/compound` 在标注 `resolution-type: T3` 的同时，必须给出 `dedup decision: skip/create/merge/delete/N/A` 双标签，4 决策是 T3 的标准 SOP（见上文）

## 与其他规则的关系

- **task-lifecycle.md**：任务生命周期的时序约束，与本规则的知识库一致性约束正交
- **anti-laziness.md 门禁 1（数量门禁）**：本规则要求「每个冲突都必须标 resolution-type（T3/T4/T5/T6/N/A 之一）」——落地了门禁 1"产出清单每项必须有明确状态"的精神
- **documentation-sync.md**：合并 / 升级 lesson 后必须同步 `memory-index.md` 的"最近教训"段
- **ADR 0002**：本规则的决策理由、权衡、备选方案记录

当前项目：`{{project_name}}`（{{project_type}} / {{language}}）
