# 协作流程

本文件补充 `AGENTS.md` 中未展开的评审和协作细节。

## 评审要点

- 行为变化是否已经写回 `docs/product.md`
- 架构变化是否已经写回 `docs/architecture.md`
- 运行方式变化是否已经写回 `docs/runbook.md`
- `docs/product.md` 里的完成标准是否已更新打勾状态
- `.agent-harness/task-log.md` 是否记录了本次改动
- 是否引入了本可脚本化但仍停留在口头层的规则

## 断路器规则

- `{{check_command}}` 或 `{{test_command}}` 连续失败 3 次后，**停止重试**。先读错误信息，分析根因，形成假设后再尝试修复。禁止盲目重复相同操作，因为连续失败说明问题不在执行层面。
- 如果修复方案超出当前知识范围，在 `.agent-harness/current-task.md` 中记录已尝试的方案和失败原因，然后告知用户需要协助。禁止猜测性修改超过 2 轮。
- 发现问题的复杂度超出预期时（如需要改 3 个以上模块），先暂停实现，更新 current-task.md 的计划，与用户确认调整后的方案。

## 渐进式实现

- 每次只改 1 个模块或 1 个功能点，验证通过后再进入下一个。禁止一次性改动多个不相关的模块，因为出错后难以定位是哪个改动导致的。
- 优先实现最小可验证版本，确认基本逻辑正确后再补充边界处理和优化。

## 额外提示

{{workflow_notes}}

{{workflow_skills_summary}}
