# 运行手册

## 主命令

- 运行：`{{run_command}}`
- 检查：`{{check_command}}`
- 测试：`{{test_command}}`
- 全量验证：`{{ci_command}}`

## 运行前应确认

- 本地依赖是否已经安装
- 环境变量和密钥是否齐备
- 当前目标环境是否为开发、预发还是生产

## 常见排障入口

- 先看 `{{check_command}}` 是否失败
- 再看 `{{test_command}}` 是否暴露行为回归
- 运行方式变更时，先更新本文件再提交

## 初始化探测备注

{{notes_bullets}}

