# AGENTS

本文件不超过 80 行。详细知识写回 `docs/` 和 `.agent-harness/project.json`，禁止在这里写百科全书。

## 开始前必读

1. `.agent-harness/current-task.md` — 有未完成任务则**从断点继续**，禁止重新开始
2. `docs/product.md` — 功能列表和完成标准的打勾状态
3. `.agent-harness/lessons.md` — 之前的教训，禁止重复犯同样的错
4. `.agent-harness/task-log.md` — 最近 5 条任务记录
5. `docs/architecture.md` — 模块边界和禁区
6. `docs/runbook.md` — 运行、测试和排障命令
7. `notes/` — 产品需求笔记（语音转文字的原始记录）。运行 `/process-notes` 可将笔记内容提取并填充到 docs/ 文档中

## 快速开始

**一键完成任务**：输入 `/lfg <任务描述>` 即可自动走完 计划→实施→评审→验证→沉淀 全流程。

**从 Issue 集成**：输入 `/lfg #42` 自动读取 Issue 内容并执行。

**不确定用什么**：输入 `/use-superpowers` 让 AI 推荐合适的技能。

## 默认工作流（手动模式）

如果不用 `/lfg`，按以下步骤手动操作：

1. 用 1 句话重述本次要改什么（不超过 25 字）。
2. 将目标和计划写入 `.agent-harness/current-task.md`。
3. 只打开与任务相关的文件，禁止全仓扫描，因为无关文件会污染上下文窗口。
4. 逐步实现。每完成 1 个功能点立即：更新 current-task.md 进度 + 对应 docs checkbox 打勾。
5. 运行 `{{check_command}}` 和 `{{test_command}}` 验证。连续失败 3 次则停下分析根因，禁止盲目重试。
6. 逐条对照 `docs/product.md` 完成标准，全部 `[x]` 才算完成。
7. 标记 current-task.md 为"待验证"，告诉用户可以验证。**不要主动清空 current-task.md**。
8. 用户确认通过后，才将 current-task.md 转移到 task-log.md 并清空。验证不通过则在当前任务里继续修。

## 硬规则

- 禁止在任务结束时才批量更新文档。每完成 1 个功能点就更新，因为会话中断会导致所有未记录进度丢失。
- 行为变化必须同步更新 `docs/product.md`，因为文档与代码不同步会误导后续所有任务。
- 架构变化必须同步更新 `docs/architecture.md`。
- 运行方式变化必须同步更新 `docs/runbook.md`。
- 禁止跳过测试直接提交。每次改动后运行 `{{test_command}}`，因为未测试代码的修复成本是即时修复的 5-10 倍。
- 禁止执行不可逆操作（rm -rf、git push --force、DROP TABLE），除非用户在当前对话中明确要求。
- 出错或返工时必须归因：根因记到 task-log、教训记到 lessons.md、缺约束导致的错误在本节新增规则。因为不归因的错误会反复出现。
- 禁止只把规则写进聊天记录或工具 memory，因为它们会被压缩丢失。长期规则必须写回仓库文件。
- 发现"待补充"占位符且已有足够信息时，直接填充，不需要等人提醒。
- 本文件中的约束不再合理时可以修改，但必须在 task-log 里记录修改原因。
{{constraints_bullets}}

## 项目快照

- 项目：`{{project_name}}`（{{project_type}}）
- 语言：`{{language}}` / `{{package_manager}}`
- 源码：{{source_paths_inline}} | 测试：{{test_paths_inline}}
