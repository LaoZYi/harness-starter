---
description: 移动端应用的性能和平台约束
paths:
  - "**/screens/**"
  - "**/components/**"
  - "**/navigation/**"
  - "**/services/**"
  - "ios/**"
  - "android/**"
  - "lib/**"
---

# 移动应用规则

本项目是移动端应用（{{project_type}}），运行在用户手机上，资源受限且网络不稳定。

## 离线优先

- 核心功能在无网络时仍可使用或给出有意义的降级提示。禁止无网时直接白屏或崩溃。
- 本地数据优先展示，后台静默同步。用户不应等网络请求完成才能看到已有数据。
- 离线操作排队，恢复网络后自动重传。冲突时优先保留用户本地修改并提示。

## 性能

- 列表渲染使用虚拟化（FlatList/RecyclerView/ListView.builder）。禁止一次性渲染全量列表，因为移动设备内存有限。
- 图片按屏幕尺寸加载���应分辨率，而不是原图缩放。使用缓存库管理内存和磁盘缓存。
- 启动时间敏感：首屏渲染不依赖网络请求。启动路径上禁止同步 I/O 或重计算。
- 主线程只做 UI 渲染。网络请求、JSON 解析、数据库操作在后台线程执行。

## 平台适配

- 使用系统原生控件和交互模式（iOS 的滑动返回、Android 的物理返回键）。禁止用自定义手势覆盖平台默认行为。
- 适配不同屏幕尺寸（手机/平板）和方向（竖屏/横屏），至少在主要页面测试。
- 权限按需申请，在需要时才弹窗，并在弹窗前说明原因。禁止启动时一次性申请所有权限。

## 包体积

- 新增依赖前评估其体积影响。移动端用户对下载大小敏感，每 MB 都影响安装率。
- 图片和资源文件使用压缩格式。大资源按需下载而非打包进安装包。
- 定期审查未使用的依赖和资源文件。

## 安全

- 敏感数据（token、密钥）存储在平台安全存储（Keychain/Keystore）中，禁止明文写入 SharedPreferences/UserDefaults。
- 网络请求全部使用 HTTPS。启用证书固定（certificate pinning）防止中间人攻击。

## Deep Linking

- 注册 URL scheme 和 Universal Links / App Links。用户从外部链接打开应用时必须导航到正确页面，而非首页。
- Deep link 参数必须做输入校验。恶意构造的 URL 不能导致崩溃或越权访问。
- 未登录用户通过 deep link 进入需要登录的页面时，先引导登录，登录后自动跳转到目标页。

## 推送通知

- 推送注册 token 变化时必须同步到后端。token 过期或用户重装后继续用旧 token 会导致送达失败。
- 用户点击推送后的跳转逻辑必须处理应用在前台、后台、已杀死三种状态。
- 推送内容禁止包含敏感信息（密码、验证码）。推送经过第三方服务器，不保证端到端加密。

## 应用更新

- 强制更新和软更新分级处理。安全漏洞修复用强制更新（拦截操作），普通功能用软更新（提示但可跳过）。
- 版本号和 build number 在 CI 中自动管理。禁止手动修改，防止提交到商店时版本冲突。
