---
description: 库项目的公开 API 稳定性和兼容性约束
paths:
  - "**/*.py"
  - "**/*.ts"
  - "**/*.js"
  - "**/*.rs"
  - "**/*.go"
  - "**/*.java"
  - "**/*.rb"
---

# 库项目 API 规则

本项目是一个**库**，其公开 API 是与外部消费者之间的契约。一旦发布，破坏性变更会影响所有依赖方。

## 公开 API 边界

- **明确区分公开和内部**：明确标注哪些模块、类、函数是公开 API（通过 `__all__`、`export`、`pub`、`public` 等），其余视为内部实现。
- **内部实现可以随意改动**，公开 API 不行。
- 在库的入口文件（`__init__.py` / `index.ts` / `lib.rs` / `mod.rs`）中维护显式导出清单，便于 review 时一眼看出 API 表面。

## 向后兼容约束

- **禁止**在非 major 版本中删除、重命名或改变公开 API 的签名（参数顺序、类型、返回值）。因为所有消费者都假定 minor/patch 版本不会破坏现有调用。
- **禁止**在非 major 版本中收紧参数类型或放宽返回类型。因为类型变化会在 TypeScript/mypy 等静态检查中表现为破坏性变更。
- **允许**在 minor 版本中新增可选参数（有默认值）、新增公开函数、新增可选字段。
- **必须**在删除 API 前经过至少一个版本的 deprecation 期，期间输出警告日志并在文档中标注 `@deprecated since X.Y`。

## 语义化版本（SemVer）纪律

- **MAJOR**：任何破坏性变更。包括：签名改变、行为改变（错误类型不同、边界条件变化）、依赖的底层行为变化。
- **MINOR**：新增功能但向后兼容。
- **PATCH**：仅修复 bug，外部可见行为不变。
- 每次发布前检查：本次变更是否真的符合声称的 bump 等级？破坏性变更隐藏在 patch 版本中是最常见的事故。

## 测试契约

- 公开 API 的每个函数必须有对应的契约测试：测试从外部角度调用，而不是从内部实现角度。
- 测试覆盖：正常路径 + 至少 1 个边界情况（空输入、极值、None）+ 至少 1 个错误路径（类型错误、非法参数）。
- 破坏性变更必须先让测试失败（作为红灯），再讨论是否真的需要破坏兼容性。

## 文档同步

- 修改公开 API 的签名、行为或错误语义时，必须同步更新 `docs/product.md`、README 中的示例代码和 API 文档。
- 示例代码必须可运行，禁止写伪代码。消费者会复制粘贴，过时的示例比没有示例更糟。

## 依赖管理

- 依赖版本限制要宽松（`>=X.Y, <Z.0`）而不是锁死（`==X.Y.Z`），否则会与消费者的依赖产生不必要的冲突。
- 新增运行时依赖前三思：每个依赖都会传染给所有消费者。开发依赖（test/lint/build）则不受此限制。
