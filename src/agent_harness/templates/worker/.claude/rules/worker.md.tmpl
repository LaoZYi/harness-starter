---
description: 异步 Worker 的可靠性和运维约束
paths:
  - "**/workers/**"
  - "**/tasks/**"
  - "**/jobs/**"
  - "**/consumers/**"
  - "**/handlers/**"
---

# Worker 规则

本项目是后台异步处理服务（{{project_type}}），消费队列消息或定时任务。

## 幂等性

- 每个任务处理函数必须设计为幂等。因为消息队列的 at-least-once 语义意味着同一条消息可能被投递多次。
- 使用业务唯一键（订单号、事件 ID）做去重判断，而不是依赖队列的 exactly-once 承诺。

## 重试和死信

- 可重试的错误（网络超时、锁竞争）和不可重试的错误（数据校验失败、业务逻辑拒绝）必须分开处理。
- 可重试错误使用指数退避策略，设置最大重试次数。超过次数后进入死信队列，不要无限重试。
- 死信队列中的消息必须保留原始上下文（原始消息体、失败原因、重试次数），便于排查和手动重放。

## 背压和资源保护

- Worker 的并发数必须可配置。禁止无限制地拉取消息，防止下游被压垮。
- 处理单条消息的超时必须设置上限。超时后释放资源并重新入队，而不是无限等待。
- 监控队列积压深度。积压超过阈值时发出告警，而不是静默等待。

## 优雅关机

- 收到终止信号（SIGTERM）时，停止拉取新消息，等待当前处理中的消息完成后再退出。
- 优雅关机有超时上限（建议 30 秒），超时后强制退出并让消息回到队列。
- 禁止在关机过程中 ack 未处理完的消息。

## 可观测性

- 每条消息的处理结果（成功/失败/重试）必须记录结构化日志，包含消息 ID 和耗时。
- 关键指标：处理吞吐量、平均延迟、失败率、队列积压深度。

## 分布式锁

- 需要独占执行的任务（定时对账、批量导出）必须使用分布式锁。禁止依赖「只部署一个实例」的假设，因为扩容时会出问题。
- 锁必须设置 TTL（建议 = 任务超时 x 1.5）。禁止无过期时间的锁，因为持有者崩溃后会永久阻塞。
- 获取锁失败时的策略必须显式选择：跳过本次执行（适合定时任务）或排队等待（适合用户触发的任务）。

## 健康检查

- Worker 进程必须暴露健康检查端点或机制（HTTP `/health`、TCP 探针、或进程管理器心跳）。编排系统需要判断 worker 是否存活。
- 健康检查应验证 worker 与队列的连接是否正常。仅返回「进程在运行」不够，连接断开意味着 worker 已失效。
- 长时间无消息处理时（超过预期间隔的 3 倍），健康检查应报告异常状态。

## 指标与告警

- 必须采集的核心指标：队列积压深度、消息处理速率（条/秒）、处理延迟（P50/P95/P99）、错误率。
- 告警阈值建议：积压深度持续增长超过 5 分钟、错误率超过 5%、P99 延迟超过 SLA 的 80%。
- 指标输出使用结构化格式（Prometheus / StatsD / 结构化日志），禁止只靠人工翻日志监控。
