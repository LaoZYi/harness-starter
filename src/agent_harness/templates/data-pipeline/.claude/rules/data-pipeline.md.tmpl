---
description: 数据管道的质量和调度约束
paths:
  - "**/dags/**"
  - "**/pipelines/**"
  - "**/transformations/**"
  - "**/models/**"
  - "**/etl/**"
---

# 数据管道规则

本项目是数据管道（{{project_type}}），负责数据的采集、转换和加载。

## 幂等重跑

- 每个管道步骤必须可安全重跑。相同输入执行两次的结果必须和执行一次完全一致。因为调度失败、上游重刷数据在生产中是常态。
- 写入目标表时使用 MERGE/UPSERT 或先删后写（按分区），而不是 INSERT APPEND。因为追加模式在重跑时会产生重复数据。

## 数据质量

- 每个步骤的输出必须有质量断言：行数非零、关键字段非空率、唯一键无重复。因为静默的数据丢失比报错更危险。
- 输入数据的 schema 变化必须被检测到（字段缺失、类型变化），而不是静默忽略或用 NULL 填充。
- 质量断言失败时阻断后续步骤，而不是继续传播脏数据。

## 调度和依赖

- 管道步���之间的依赖必须显式声明（DAG 定义），禁止用"先等 30 分钟"这种时间依赖替代数据依赖。
- 每个步骤有超时设置。超时后自动失败并通知，而不是无限运行占用资源。
- 回填（backfill）场景：管道必须接受日期范围参数，而不是只处理"今天的数据"。

## Schema 演进

- 上游 schema 变更时，管道应能检测到并给出明确报错。禁止静默丢弃新字段或在旧字段消失时崩溃。
- 下游表的 schema 变更（加列、改类型）必须通过 migration 管理。禁止在管道代码中直接 ALTER TABLE。
- 保留原始数据层（raw/staging），转换在下游层执行。原始层只做 schema-on-read，不做清洗。

## 可观测性

- 每次运行记录：开始时间、结束时间、处理行数、输入/输出数据量。
- 数据延迟监控：从源系统产生数据到管道处理完成的端到端延迟。
- 失败时的错误信息必须包含失败的步骤名、输入数据范围和具体错误原因。
