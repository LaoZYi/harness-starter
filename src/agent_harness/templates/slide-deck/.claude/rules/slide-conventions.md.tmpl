---
description: 幻灯片项目（Marp / Reveal.js / 内部演示文稿）的写作约定和定稿契约
paths:
  - "**/*.md"
  - "**/*.markdown"
  - "**/slides.md"
  - "**/deck.md"
---

# 幻灯片项目规则

本项目是幻灯片项目（{{project_type}}），主要产出 Marp / Reveal.js / 类似 markdown 形态的演示文稿，每页一标题、要点扁平、附演讲稿。

## 页面结构

- 每页 = 一级标题 `#` 起始，**不**用 `##` 嵌套子页（嵌套用「分页符」`---` 隔开下一页）
- 每页要点用 `-` 列表，**最多 5 条**（信息密度上限，超过则拆页）
- 标题用陈述性短语，不用问句、感叹句
- 同级标题命名风格一致（要么全是名词短语，要么全是动宾短语）

## 演讲稿（presenter notes）

每页**必须**附演讲稿块，定稿期不允许留空：

```markdown
# 页标题

- 要点 1
- 要点 2

<!-- speaker:
这页讲 X。重点强调 Y。如果听众问 Z，参考第 N 页。
-->
```

- Marp 项目用 `<!-- speaker: ... -->` HTML 注释
- Reveal.js 项目用 `Note:` 行
- 演讲稿是讲者口播脚本，不是幻灯片可见内容
- `/finalize-slide` 8 项必检会硬卡「演讲稿全部填充」

## 视觉占位

- 图片：`![alt](placeholder)` —— 草稿期占位，定稿期必须解析为真实图片路径或外部 URL
- 数据图表：`![chart:metric-name](placeholder)` + 末尾「数据来源」段
- 视觉留白：单页内容超过预期时，加 `<!-- visual: split -->` 标记建议拆页（评审时审）

## 字数与页数

- 页数估算 = outline 总页数 ±10%（不用「字数」，幻灯片按页计）
- 单页文字 ≤ 100 中文字符 / ≤ 50 英文字符（信息密度限制，听众读不过来）
- 引用 / 表格 / 图片不计入文字数

## 术语表

- 根目录维护 `glossary.md`，所有专业术语首次出现的定义都进这里
- 术语首次出现时**在演讲稿里展开**，幻灯片可见处保留缩写（保持视觉简洁）
- 大小写归一：`HTTP` 不写 `http`、`Kubernetes` 不写 `k8s`
- 见项目 `CLAUDE.md` 的「术语大小写扩展清单」

## 引用格式

- 图表 / 数据来源：末尾「来源」单页或脚注
- 引用占位 `[ref:X]` —— 草稿期允许，定稿期必须解析
- 外部 URL：放演讲稿里，不放幻灯片可见处（视觉污染）

## 版本与日期

每个 deck 头部 frontmatter 含：

```yaml
---
title: <演讲标题>
version: 1.0
date: YYYY-MM-DD
status: draft | review | final
authors: [<作者>]
audience: <目标听众>
duration_min: <分钟数>
---
```

- `status: draft` —— 草稿期，允许占位符存在
- `status: review` —— 已交评审，开始 P0/P1 修复
- `status: final` —— 通过 `/finalize-doc` 8 项必检（slide 版）后才能改为 final

## 评审维度（slide 特有）

`/review-doc` 4 人格 + slide 加 3 维：

- 视觉留白：单页是否过密 / 字号是否够 / 留白是否合理
- 演讲节奏：每页停留时长是否匹配 duration_min
- 信息密度：每页 ≤ 5 要点是否守住

本期评审维度仅写入 `/lfg-slide` 流水线注释，不传 context 给 `/review-doc`（D 方案）。

## 不做的事

- **不**在幻灯片正文写完整段落（拆要点 + 演讲稿展开）
- **不**用 `/lfg`（写代码）；用 `/lfg-slide`
- **不**用 `/lfg-doc`（写文档）；slide 跟 doc 评审 / 定稿语义不同
- **不**在 `/finalize-doc` 通过前发布对外
- **不**在幻灯片可见处放敏感信息（演讲者会忘记隐藏）

## 一句话总结

**幻灯片是讲者的辅助，不是演讲稿本身——每页 5 条要点 + 演讲稿展开 + 视觉留白，听众扫一眼就知道这页讲什么。**
