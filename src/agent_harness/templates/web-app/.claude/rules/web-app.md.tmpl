---
description: 前端 Web 应用架构和质量约束
paths:
  - "**/components/**"
  - "**/pages/**"
  - "**/views/**"
  - "**/layouts/**"
  - "**/styles/**"
  - "**/hooks/**"
  - "**/composables/**"
---

# Web 应用规则

本项目是 Web 前端应用（{{project_type}}），直接面向用户浏览器。

## 组件架构

- 组件按职责分离：展示组件（纯 UI，通过 props 接收数据）和容器组件（管理状态和副作用）。因为混合职责的组件难以测试和复用。
- 共享组件放在统一目录（`components/`），页面级组件和页面放在一起。禁止跨页面直接引用另一页面的内部组件。

## 状态管理

- 区分本地状态（组件内）和全局状态（store/context）。只有跨多个组件共享的数据才放到全局状态。
- 服务端数据优先用数据获取层（SWR/React Query/useFetch）管理，而非手动塞进全局 store。因为缓存、过期、重试逻辑不应重复实现。

## 性能

- 图片使用现代格式（WebP/AVIF）和懒加载。因为图片通常是页面最大的传输体积。
- 避免在渲染路径上做同步阻塞操作。大列表用虚拟滚动，大计算用 Web Worker 或分片。
- 路由级代码分割：每个路由只加载自己需要的 JS 和 CSS。

## 无障碍（a11y）

- 交互元素必须有语义化标签（`button` 不用 `div`，`a` 有 `href`）。因为屏幕阅读器和键盘用户依赖正确的语义。
- 表单控件必须关联 `label`，错误信息使用 `aria-describedby` 关联到对应字段。
- 颜色不能是传达信息的唯一手段（同时使用图标/文字）。

## 浏览器兼容

- CSS 和 JS 的目标浏览器范围在构建配置中显式声明（browserslist / target）。禁止假设"现代浏览器都支持"。
- 使用新 API 前检查 Can I Use，必要时加 polyfill 或降级方案。

## 错误边界

- 组件树中必须有错误边界（React ErrorBoundary / Vue errorCaptured），防止单个组件崩溃导致整页白屏。
- 错误边界捕获后展示友好的降级 UI（「出了点问题，请刷新」），同时上报错误到监控。
- 异步操作（数据获取、表单提交）的失败状态必须有对应的 UI 反馈，不能静默吞掉。

## 包体积预算

- 设定 JS bundle 体积预算（如首屏 < 200KB gzip）。CI 中集成 bundlesize 或类似工具自动卡线。
- 新增依赖前用 bundlephobia 评估体积影响。超过 50KB 的依赖需要说明必要性。
- 使用 tree-shaking 友好的 ESM 导入。禁止 `import _ from 'lodash'`，改用 `import debounce from 'lodash/debounce'`。

## SSR / SSG 考量

- 服务端渲染的组件中禁止直接访问 `window`/`document`/`localStorage`。浏览器 API 必须在 `useEffect`/`onMounted` 中使用或做 SSR 安全检查。
- 需要 SEO 的页面使用 SSR 或 SSG。纯交互页面可以用 CSR。在路由配置中标注渲染策略。

## 检查清单（按需参考）

- 新增 UI 组件或交互前，查阅 `.agent-harness/references/accessibility-checklist.md`（键盘导航、屏幕阅读、ARIA、WCAG 2.1 AA）
- 部署前或调优性能时，查阅 `.agent-harness/references/performance-checklist.md`（LCP/INP/CLS、图像、JS bundle）
- 可用 `/recall <关键词> --refs` 定向检索，避免全文读入上下文
