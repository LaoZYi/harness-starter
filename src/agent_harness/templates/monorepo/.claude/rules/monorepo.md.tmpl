---
description: Monorepo 的依赖边界和工作区管理约束
paths:
  - "**/packages/**"
  - "**/apps/**"
  - "**/libs/**"
  - "turbo.json"
  - "pnpm-workspace.yaml"
  - "lerna.json"
  - "nx.json"
---

# Monorepo 规则

本项目是 Monorepo（{{project_type}}），多个包/应用共存于一个仓库中。

## 依赖边界

- 包之间的依赖必须通过包管理器的 workspace 协议显式声明。禁止跨包直接引用相对路径文件。因为隐式依赖会破坏独立构建和发布。
- 依赖方向必须单向：底层库不依赖上层应用��出现循环依赖时，提取共享部分到新包。
- 每个包有自己的 `package.json`/`Cargo.toml`/`pyproject.toml`，显式列出依赖。禁止"反正都在一个仓库里"就跳过声明。

## 共享代码

- 跨包复用的代码提取到独立的 shared/common 包，通过正式的包引用方式使用。
- 共享包的 API 变更视为破坏性变更。修改前确认所有消费方能适配。
- 工具链配置（ESLint、TypeScript、测试）尽量在根级统一，各包只做必要的覆盖。

## 变更影响范围

- 提交前确认变更只影响预期的包。使用构建工具的 affected 检测（`turbo run --filter=...[HEAD]`、`nx affected`）验证影响范围。
- 共享包的改动必须运行所有消费方的测试，不能只跑本包的测试。
- PR 描述中标注影响到哪些包，方便 reviewer 理解范围。

## 版本和发布

- 各包独立版本号。共享库发布 minor/patch 时，消费方不应被迫同步升级。
- 使用 changeset 或类似工具管理版本 bump。禁止手动编辑多个 `package.json` 的版本号。
- 发布顺序：先发布被依赖的底层包，再发布上层包。构建工具的拓扑排序可自动处理。

## 构建和 CI

- CI 使用增量构建：只构建和测试被变更影响到的包。全量构建作为定时任务而非每次 PR 触发。
- 构建缓存（turbo cache / nx cache）必须启用。缓存键包含源码哈希和依赖版本。
