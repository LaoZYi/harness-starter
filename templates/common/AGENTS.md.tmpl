# AGENTS

把这个文件当成入口，不要把它写成百科全书。长期知识统一写回 `docs/` 和 `.agent-harness/project.json`。

## 开始前先看什么

1. `.agent-harness/current-task.md`，如果有未完成任务则从断点继续。
2. `docs/product.md`，看功能列表和完成标准的打勾状态。
3. `.agent-harness/lessons.md`，了解之前踩过的坑，避免再犯。
4. `.agent-harness/task-log.md`，了解最近的任务和决策。
5. `docs/architecture.md`，理解模块边界和禁区。
6. `docs/runbook.md`，理解运行、测试和排障入口。

## 默认工作流

1. 先用一句话重述本次要改什么。
2. 将任务目标和计划写入 `.agent-harness/current-task.md`。
3. 只打开和任务相关的文件，不要全仓乱扫。
4. 逐步实现，每完成一个功能点立即：更新 current-task.md 进度 + 对应 docs 的 checkbox 打勾。
5. 用 `{{check_command}}` 验证。
6. **逐条对照 `docs/product.md` 完成标准**，全部 `[x]` 才算完成。
7. 将 current-task.md 转移到 task-log.md，清空 current-task.md。

## 硬规则

- 行为变化必须同步更新 `docs/product.md`。
- 架构变化必须同步更新 `docs/architecture.md`。
- 运行方式变化必须同步更新 `docs/runbook.md`。
- **文档更新要增量进行**：每完成一个功能点就更新，不要堆到任务结束。
- 每次完成任务后，检查 `docs/` 和本文件是否需要同步更新，如有需要直接更新，不需要等人提醒。
- 如果发现文档中有"待补充"占位符且已有足够信息，直接填充。
- 用户否决了某个功能或方案时，从 `docs/product.md` 中移除对应条目，并在 task-log 里记录原因。
- 发现本文件中的约束不再合理时，可以修改，但必须在 task-log 里记录修改原因。
- 出错或返工时必须归因：记录根因到 task-log、提炼教训到 lessons.md、必要时在本文件新增规则。
- 长期规则不要只写进聊天记录或工具 memory。
{{constraints_bullets}}

## 当前项目快照

- 项目名：`{{project_name}}`
- 项目类型：`{{project_type}}`
- 主要语言：`{{language}}`
- 包管理器：`{{package_manager}}`
- 源码目录：{{source_paths_inline}}
- 测试目录：{{test_paths_inline}}
