---
description: API 端点和接口变更约束
paths:
  - "**/routes/**"
  - "**/handlers/**"
  - "**/api/**"
  - "**/controllers/**"
  - "**/endpoints/**"
---

# API 规则

- 修改 API 端点的请求参数、响应格式或状态码时，必须同步更新 `docs/product.md` 中的接口说明。因为接口变更影响所有调用方，文档不同步会导致集成失败。
- 禁止删除或重命名已发布的 API 端点，除非用户确认没有外部调用方。因为破坏性变更会导致客户端崩溃。
- 所有用户输入必须在入口处校验。禁止信任客户端传入的数据直接用于数据库查询或文件操作。
- 错误响应必须返回结构化格式（状态码 + 错误信息），禁止返回裸字符串或暴露内部堆栈。
- 新增端点必须同时补充对应的测试用例，覆盖正常路径和至少 1 个错误路径。
