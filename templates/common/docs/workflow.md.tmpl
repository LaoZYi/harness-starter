# 协作流程

## 推荐流程

1. 先从 `AGENTS.md` 和 `.agent-harness/project.json` 理解项目上下文。
2. 小步修改代码。
3. 同步更新 `docs/`。
4. 先跑 `{{check_command}}`，再跑 `{{test_command}}`。
5. 需要完整收口时跑 `{{ci_command}}`。

## 评审要点

- 行为变化是否已经写回 `docs/product.md`
- 架构变化是否已经写回 `docs/architecture.md`
- 运行方式变化是否已经写回 `docs/runbook.md`
- 是否引入了本可脚本化但仍停留在口头层的规则

## 额外提示

{{workflow_notes}}

