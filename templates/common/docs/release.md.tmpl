# 发布清单

当前部署目标：`{{deploy_target}}`
当前环境状态：{{production_status}}

## 发布前检查

{{release_checks_bullets}}

## 通用收口

1. `{{ci_command}}` 是否通过。
2. 运行和排障入口是否仍准确。
3. 行为变化是否已经同步到 `docs/product.md`。
4. 回滚方式是否明确。

