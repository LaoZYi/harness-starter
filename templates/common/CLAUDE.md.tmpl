# Claude Code Notes

本仓库的长期规则以 `AGENTS.md` 为入口，以 `docs/` 和 `.agent-harness/project.json` 为知识源。

开始任务时请先读：

1. `AGENTS.md`
2. `.agent-harness/current-task.md`，如果有进行中的任务则**继续完成**，不要重新开始
3. `docs/product.md`，看功能列表和完成标准的打勾状态
4. `.agent-harness/lessons.md`，了解之前踩过的坑，避免再犯
5. `.agent-harness/task-log.md` 的最近 5 条记录（不需要全部读完）

## Checkpoint 规则（防止长任务中断丢失进度）

开始一个新任务时：

1. 将任务目标和计划步骤写入 `.agent-harness/current-task.md`
2. 每完成一个步骤，更新 current-task.md 的进度和已做的改动
3. **每完成一个独立功能点就立即更新 `docs/product.md` 的对应 checkbox**，不要堆到最后
4. 任务全部完成后，将 current-task.md 的内容转移到 task-log.md，然后清空 current-task.md

如果开会话时发现 current-task.md 有未完成的任务（上次中断了），先读懂进度，然后**从断点继续**。

## Harness 自维护规则

每完成一个功能点（不是等整个任务结束）就立即更新对应文档：

- 新增功能 → 在 `docs/product.md` 功能列表里追加条目
- 用户要求移除或修改功能 → 在 `docs/product.md` 里删除或修改对应条目和完成标准
- 改了文件结构 → 更新 `docs/architecture.md`
- 改了运行方式 → 更新 `docs/runbook.md`
- 改了协作约束 → 更新 `AGENTS.md` 对应节
- 发现某条约束不再合理 → 在 `AGENTS.md` 里修改并在 task-log 里记录修改原因
- 如果 `docs/` 中有"待补充"字样且已有足够信息，直接填充

## 交付验收规则

完成实现后，**必须**打开 `docs/product.md`，逐条对照：

1. **功能列表**：每一条功能是否都已实现？遗漏的必须补完，不要留半成品。
2. **完成标准**：逐条验证，通过的把 `- [ ]` 改为 `- [x]`，未通过的必须修复后再打勾。

只有完成标准全部变为 `[x]`，才能告诉用户"已完成"。

## Task Log 规则

任务完成后，将 current-task.md 的内容整理为一条记录追加到 `.agent-harness/task-log.md`：

```
## YYYY-MM-DD 任务简述

- 需求：用户原话或一句话概括
- 做了什么：具体改动摘要
- 关键决策：为什么选了这个方案（如有取舍）
- 改了：涉及的文件列表
- 完成标准：本次新通过的 checkbox
```

### Task Log 归档

当 task-log.md 超过 50 条记录时，将较早的记录移到 `.agent-harness/task-log-archive.md`，task-log.md 只保留最近 20 条。归档时在 task-log.md 顶部注明"更早的记录见 task-log-archive.md"。

## 错误归因规则

当出现以下情况时，**必须**执行错误归因：

- 用户指出实现有误或不满意
- 自检发现与完成标准不符
- 发现之前的改动引入了 bug

归因流程：

1. **记录返工日志**到 task-log.md：

```
## YYYY-MM-DD 返工：xxx

- 用户反馈：原话
- 根因：为什么会出这个错（缺了什么认知/忽略了什么约束/误解了什么需求）
- 修复：具体改了什么
- 改了：涉及的文件列表
```

2. **提炼教训**到 `.agent-harness/lessons.md`：

```
### 教训标题

- 错误：当时做了什么错误的事
- 根因：为什么会犯这个错
- 规则：以后遇到类似情况应该怎么做
```

3. **判断是否需要新增硬规则**：如果这个错误是因为缺少约束导致的（而不是单次失误），在 `AGENTS.md` 硬规则里追加一条防止再犯的规则，并在 task-log 里说明为什么加了这条规则。

不要把新的长期规则只写在这里。
